# Story 16.5 — UI membro: secao de conquistas no perfil

> **Epic 16 — Gamificacao**

## Status: Ready for Review

## Executor Assignment

| Metadata        | Value                          |
|-----------------|--------------------------------|
| Work Type       | Code / UI / Components         |
| Primary Executor| @dev                           |
| Quality Gate    | @qa                            |
| Estimated Effort| ~3 horas                       |
| Risk            | Low (componente novo, sem modificacao de existentes) |

## Dependencies

| Dependency | Story | Status |
|------------|-------|--------|
| API GET /api/gamification/badges | 16.4 | Pendente |
| API GET /api/gamification/profile | 16.4 | Pendente |

## Story

**Como** membro da plataforma,
**Quero** ver meus badges e conquistas em uma secao dedicada no dashboard/perfil,
**Para que** eu possa acompanhar meu progresso e me sentir motivado a continuar estudando.

## Acceptance Criteria

1. **AC1** — Componente `AchievementsSection` criado em `src/components/member/AchievementsSection.tsx` exibindo grid de badges.
2. **AC2** — Cada badge exibe: icone (icon_url ou fallback), nome, descricao (tooltip ou abaixo do nome), estado locked/unlocked, data de desbloqueio (se unlocked).
3. **AC3** — Badges desbloqueados exibidos em cores normais; badges locked exibidos em grayscale com opacidade reduzida.
4. **AC4** — Grid responsivo: 3 colunas em desktop, 2 em tablet, 1 em mobile.
5. **AC5** — Skeleton loading enquanto dados carregam (shimmer animation existente).
6. **AC6** — Estado vazio: mensagem amigavel quando nao ha badges cadastrados.
7. **AC7** — Componente integrado na pagina de dashboard do membro (`app/(member)/` dashboard ou perfil).
8. **AC8** — Usa fetch para `GET /api/gamification/badges` para obter dados.
9. **AC9** — Segue design system Netflix-inspired (dark theme, cores do globals.css).

## Tasks / Subtasks

- [x] **T1** — Criar componente `BadgeCard` em `src/components/member/BadgeCard.tsx` para exibir um badge individual -> (AC2, AC3)
- [x] **T2** — Criar componente `AchievementsSection` em `src/components/member/AchievementsSection.tsx` com grid de badges -> (AC1, AC4, AC5, AC6)
- [x] **T3** — Criar skeleton `AchievementsSkeleton` para loading state -> (AC5)
- [x] **T4** — Integrar `AchievementsSection` na pagina do membro -> (AC7, AC8)
- [x] **T5** — Testes unitarios para BadgeCard e AchievementsSection -> (AC2, AC3)

## Dev Notes

### Relevant Source Tree

```
packages/memberly-app/
├── src/
│   ├── app/(member)/
│   │   └── page.tsx                        # (MODIFICAR) ou dashboard, integrar AchievementsSection
│   ├── components/
│   │   └── member/
│   │       ├── AchievementsSection.tsx      # (NOVO) Grid de badges
│   │       ├── BadgeCard.tsx               # (NOVO) Badge individual
│   │       └── ProductCard.tsx             # Referencia: card pattern
│   └── types/
│       └── database.ts                     # Badge type
├── tests/
│   └── unit/components/member/
│       ├── AchievementsSection.test.tsx     # (NOVO)
│       └── BadgeCard.test.tsx              # (NOVO)
```

### BadgeCard Component Pattern

```typescript
// src/components/member/BadgeCard.tsx
'use client';

interface BadgeWithStatus {
  id: string;
  name: string;
  description: string;
  icon_url: string | null;
  unlocked: boolean;
  unlocked_at: string | null;
}

export function BadgeCard({ badge }: { badge: BadgeWithStatus }) {
  return (
    <div
      className={`
        rounded-xl border border-dark-border bg-dark-card p-4 text-center
        transition-all duration-300
        ${badge.unlocked
          ? 'border-primary/30 shadow-lg shadow-primary/10'
          : 'opacity-50 grayscale'
        }
      `}
    >
      <div className="mx-auto mb-3 flex h-16 w-16 items-center justify-center rounded-full bg-dark-surface text-3xl">
        {badge.icon_url ? (
          <img src={badge.icon_url} alt={badge.name} className="h-10 w-10" />
        ) : (
          <span>🏅</span>
        )}
      </div>
      <h3 className="text-sm font-semibold text-white">{badge.name}</h3>
      <p className="mt-1 text-xs text-gray-400">{badge.description}</p>
      {badge.unlocked && badge.unlocked_at && (
        <p className="mt-2 text-xs text-primary">
          {new Date(badge.unlocked_at).toLocaleDateString('pt-BR')}
        </p>
      )}
    </div>
  );
}
```

### AchievementsSection Component

```typescript
// src/components/member/AchievementsSection.tsx
'use client';

import { useEffect, useState } from 'react';
import { BadgeCard } from './BadgeCard';

export function AchievementsSection() {
  const [badges, setBadges] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetch('/api/gamification/badges')
      .then((res) => res.json())
      .then((data) => setBadges(data.data))
      .finally(() => setLoading(false));
  }, []);

  if (loading) return <AchievementsSkeleton />;
  if (!badges.length) return <EmptyState />;

  return (
    <section>
      <h2 className="mb-4 text-xl font-bold text-white">Conquistas</h2>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {badges.map((badge) => (
          <BadgeCard key={badge.id} badge={badge} />
        ))}
      </div>
    </section>
  );
}
```

### Design System References

- Background: `bg-dark-card` (#2a2a2a), `bg-dark-surface` (#1a1a1a)
- Primary accent: `text-primary` (#e50914)
- Text: `text-white`, `text-gray-400`
- Border: `border-dark-border`
- Skeleton: usar `animate-shimmer` existente (ver globals.css)
- Cards: `rounded-xl` com border e shadow (seguir ProductCard pattern)

### Edge Cases

- **Sem badges cadastrados:** Exibir mensagem "Nenhuma conquista disponivel ainda"
- **Badge sem icon_url:** Exibir fallback emoji
- **Erro de fetch:** Exibir estado de erro com retry
- **Muitos badges:** Grid ajusta automaticamente, scroll natural da pagina

### Testing

- **BadgeCard:** Renderizar com badge unlocked (cores normais), renderizar com badge locked (grayscale)
- **AchievementsSection:** Mock fetch, renderizar grid, verificar skeleton durante loading, verificar empty state
- **Responsividade:** Verificar classes de grid responsivo

## Change Log

| Data       | Versao | Descricao     | Autor  |
|------------|--------|---------------|--------|
| 2026-03-20 | 0.1    | Draft inicial | @sm    |
| 2026-03-20 | 0.2 | Validação PO: aprovada | @po |
| 2026-03-20 | 0.3 | Implementação completa: BadgeCard, AchievementsSection, AchievementsSkeleton, integração no dashboard, testes (16/16 passing) | @dev |

## Dev Agent Record

| Item | Detail |
|------|--------|
| Agent | @dev (Dex) |
| Status | Ready for Review |
| Tests | 16/16 passing (BadgeCard: 9, AchievementsSection: 7) |
| Typecheck | Clean (tsc --noEmit) |

### Files Created
- `packages/memberly-app/src/components/member/BadgeCard.tsx`
- `packages/memberly-app/src/components/member/AchievementsSkeleton.tsx`
- `packages/memberly-app/src/components/member/AchievementsSection.tsx`
- `packages/memberly-app/tests/unit/components/member/BadgeCard.test.tsx`
- `packages/memberly-app/tests/unit/components/member/AchievementsSection.test.tsx`

### Files Modified
- `packages/memberly-app/src/app/(member)/page.tsx` — Added AchievementsSection import and integration

## QA Results

| Item | Result |
|------|--------|
| QA Agent | @qa (Quinn) |
| Date | 2026-03-20 |
| Verdict | **PASS** |

### AC Verification

| AC | Status | Notes |
|----|--------|-------|
| AC1 | PASS | `AchievementsSection` created at `src/components/member/AchievementsSection.tsx`, renders grid of `BadgeCard` components |
| AC2 | PASS | `BadgeCard` displays icon (icon_url or fallback emoji), name, description, locked/unlocked state, and unlock date when unlocked |
| AC3 | PASS | Unlocked badges get `border-primary/30 shadow-primary/10`; locked badges get `opacity-50 grayscale` — verified by unit tests |
| AC4 | PASS | Grid uses `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` — responsive breakpoints confirmed in test |
| AC5 | PASS | `AchievementsSkeleton` component uses `Skeleton` with `animate-shimmer` — verified by test |
| AC6 | PASS | Empty state renders "Nenhuma conquista disponivel ainda" message — verified by test |
| AC7 | PASS | `AchievementsSection` integrated in `app/(member)/page.tsx` member dashboard |
| AC8 | PASS | Fetches from `GET /api/gamification/badges` via `useEffect` — verified by test |
| AC9 | PASS | Uses `bg-dark-card`, `border-dark-border`, `text-primary`, `text-white`, `text-gray-400` — follows Netflix dark theme |

### Quality Checks

- [x] All 16/16 tests pass (BadgeCard: 9, AchievementsSection: 7)
- [x] TypeScript clean (tsc --noEmit: 0 errors)
- [x] Absolute imports used (`@/components/...`, `@/lib/...`)
- [x] Error state with retry button implemented (beyond AC requirements — good)
- [x] Sorting: unlocked badges shown before locked badges (good UX addition)
- [x] `cn()` utility used for conditional className merging
- [x] No design system violations
