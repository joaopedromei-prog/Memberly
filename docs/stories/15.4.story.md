# Story 15.4 — UI: Bell Icon + Dropdown no Header do Membro

> **Epic 15 — Notificações In-App**

## Status: Ready for Review

## Executor Assignment

| Metadata        | Value                          |
|-----------------|--------------------------------|
| Work Type       | Code / UI / Components         |
| Primary Executor| @dev                           |
| Quality Gate    | @qa                            |
| Estimated Effort| ~3 horas                       |
| Risk            | Medium (modifica MemberHeader existente, polling state) |

## Story

**Como** membro autenticado,
**Quero** ver um ícone de sino no header com badge de contagem de notificações não-lidas e um dropdown com as últimas notificações,
**Para que** eu possa rapidamente visualizar atualizações e navegar para o conteúdo relevante sem sair da página atual.

## Acceptance Criteria

1. **AC1** — Ícone de sino (Bell) exibido no `MemberHeader` entre o botão de busca e o nome do usuário, usando `lucide-react` icon `Bell`.
2. **AC2** — Badge numérico sobre o sino mostrando contagem de notificações não-lidas. Badge vermelho (`#e50914`) com texto branco. Escondido quando count = 0. Mostra "9+" quando count > 9.
3. **AC3** — Clicar no sino abre dropdown/painel posicionado abaixo do sino, mostrando as últimas 10 notificações.
4. **AC4** — Cada notificação no dropdown exibe: ícone por tipo (BookOpen para NEW_LESSON, MessageCircle para COMMENT_REPLY, Trophy para COURSE_COMPLETED), título, body preview (truncado em 1 linha), tempo relativo ("há 5 min", "há 2h"), styling diferenciado para lida/não-lida (não-lida com fundo levemente mais claro e borda esquerda vermelha).
5. **AC5** — Clicar em uma notificação: marca como lida via `PATCH /api/notifications/[id]`, fecha o dropdown, navega para a página relevante baseada em `notification.data` (NEW_LESSON → `/products/{slug}/lessons/{lessonId}`, COMMENT_REPLY → `/products/{slug}/lessons/{lessonId}`, COURSE_COMPLETED → `/products/{slug}`).
6. **AC6** — Botão "Marcar todas como lidas" no footer do dropdown, chama `POST /api/notifications/mark-all-read`, atualiza o badge para 0.
7. **AC7** — Link "Ver todas" no footer do dropdown que navega para `/notifications`.
8. **AC8** — Polling a cada 30 segundos para `GET /api/notifications/unread-count` para atualizar o badge automaticamente.
9. **AC9** — Dropdown fecha ao clicar fora (click outside) ou pressionar Escape.
10. **AC10** — Animação de entrada do dropdown usando `motion/react` (fade + slide down).
11. **AC11** — Estado de loading (skeleton) enquanto carrega notificações no dropdown.
12. **AC12** — Estado vazio no dropdown quando não há notificações: "Nenhuma notificação".

## Tasks / Subtasks

- [x] **T1** — Criar componente `src/components/member/NotificationBell.tsx` com ícone sino + badge de contagem → (AC1, AC2)
- [x] **T2** — Criar componente `src/components/member/NotificationDropdown.tsx` com lista de notificações, estados empty/loading → (AC3, AC4, AC11, AC12)
- [x] **T3** — Implementar click handler: mark as read + navigate → (AC5)
- [x] **T4** — Implementar "Marcar todas como lidas" e link "Ver todas" no footer → (AC6, AC7)
- [x] **T5** — Implementar polling de unread-count a cada 30s com `useEffect` + `setInterval` → (AC8)
- [x] **T6** — Implementar click outside e Escape para fechar dropdown → (AC9)
- [x] **T7** — Adicionar animação de entrada com `motion/react` → (AC10)
- [x] **T8** — Integrar `NotificationBell` no `MemberHeader.tsx` entre busca e nome do usuário → (AC1)
- [x] **T9** — Criar hook `src/hooks/useNotifications.ts` para encapsular fetch, polling, mark read → (AC8)
- [x] **T10** — Testes unitários para NotificationBell, NotificationDropdown, useNotifications → (todos ACs)

## Dev Notes

### Relevant Source Tree

```
packages/memberly-app/
├── src/
│   ├── components/member/
│   │   ├── MemberHeader.tsx                # (MODIFICAR) Adicionar NotificationBell
│   │   ├── NotificationBell.tsx            # (NOVO) Bell icon + badge
│   │   └── NotificationDropdown.tsx        # (NOVO) Dropdown panel
│   ├── hooks/
│   │   └── useNotifications.ts             # (NOVO) Notification data hook
│   ├── lib/
│   │   ├── notifications/
│   │   │   └── types.ts                    # NOTIFICATION_TYPES (criado em 15.3)
│   │   └── utils/
│   │       └── relative-time.ts            # Existing: formatRelativeTime
│   └── app/(member)/
│       └── notifications/
│           └── page.tsx                    # (Story 15.5 — link target)
├── tests/
│   └── unit/components/member/
│       ├── NotificationBell.test.tsx        # (NOVO)
│       └── NotificationDropdown.test.tsx    # (NOVO)
```

### MemberHeader Integration Point

Inserir `NotificationBell` no header entre o botão de busca e o nome do usuário:

```tsx
// MemberHeader.tsx — current structure
<div className="flex items-center gap-2 md:gap-4">
  <button onClick={() => setIsSearchOpen(true)} ...>
    <Search size={20} />
  </button>
  {/* INSERT NotificationBell HERE */}
  {user && <span ...>{user.full_name}</span>}
  <button onClick={handleLogout} ...>Sair</button>
</div>
```

### NotificationBell Component Pattern

```tsx
'use client';

import { Bell } from 'lucide-react';
import { useState, useRef, useEffect } from 'react';
import { AnimatePresence, motion } from 'motion/react';
import { NotificationDropdown } from './NotificationDropdown';
import { useNotifications } from '@/hooks/useNotifications';

export function NotificationBell() {
  const { unreadCount, notifications, isLoading, markAsRead, markAllAsRead, refresh } = useNotifications();
  const [isOpen, setIsOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  // Click outside
  useEffect(() => {
    function handleClickOutside(e: MouseEvent) {
      if (ref.current && !ref.current.contains(e.target as Node)) {
        setIsOpen(false);
      }
    }
    document.addEventListener('mousedown', handleClickOutside);
    return () => document.removeEventListener('mousedown', handleClickOutside);
  }, []);

  // Escape key
  useEffect(() => {
    function handleEscape(e: KeyboardEvent) {
      if (e.key === 'Escape') setIsOpen(false);
    }
    document.addEventListener('keydown', handleEscape);
    return () => document.removeEventListener('keydown', handleEscape);
  }, []);

  return (
    <div ref={ref} className="relative">
      <button onClick={() => { setIsOpen(!isOpen); if (!isOpen) refresh(); }} ...>
        <Bell size={20} />
        {unreadCount > 0 && (
          <span className="absolute -top-1 -right-1 bg-primary text-white text-[10px] font-bold rounded-full min-w-[18px] h-[18px] flex items-center justify-center">
            {unreadCount > 9 ? '9+' : unreadCount}
          </span>
        )}
      </button>
      <AnimatePresence>
        {isOpen && (
          <motion.div
            initial={{ opacity: 0, y: -8 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -8 }}
            transition={{ duration: 0.15 }}
            className="absolute right-0 top-full mt-2 w-80 md:w-96 ..."
          >
            <NotificationDropdown
              notifications={notifications}
              isLoading={isLoading}
              onMarkRead={markAsRead}
              onMarkAllRead={markAllAsRead}
              onClose={() => setIsOpen(false)}
            />
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
}
```

### useNotifications Hook Pattern

```typescript
// src/hooks/useNotifications.ts
export function useNotifications() {
  const [unreadCount, setUnreadCount] = useState(0);
  const [notifications, setNotifications] = useState([]);
  const [isLoading, setIsLoading] = useState(false);

  // Poll unread count every 30s
  useEffect(() => {
    fetchUnreadCount();
    const interval = setInterval(fetchUnreadCount, 30_000);
    return () => clearInterval(interval);
  }, []);

  async function fetchUnreadCount() {
    const res = await fetch('/api/notifications/unread-count');
    const data = await res.json();
    setUnreadCount(data.count);
  }

  async function refresh() {
    setIsLoading(true);
    const res = await fetch('/api/notifications?limit=10');
    const data = await res.json();
    setNotifications(data.notifications);
    setIsLoading(false);
  }

  // markAsRead, markAllAsRead...
  return { unreadCount, notifications, isLoading, markAsRead, markAllAsRead, refresh };
}
```

### Notification Type Icons

```typescript
import { BookOpen, MessageCircle, Trophy } from 'lucide-react';

const NOTIFICATION_ICONS = {
  NEW_LESSON: BookOpen,
  COMMENT_REPLY: MessageCircle,
  COURSE_COMPLETED: Trophy,
} as const;
```

### Navigation Mapping

```typescript
function getNotificationUrl(notification: Notification): string {
  const data = notification.data as Record<string, string>;
  switch (notification.type) {
    case 'NEW_LESSON':
      return `/products/${data.productSlug}/lessons/${data.lessonId}`;
    case 'COMMENT_REPLY':
      return `/products/${data.productSlug}/lessons/${data.lessonId}`;
    case 'COURSE_COMPLETED':
      return `/products/${data.productSlug}`;
    default:
      return '/notifications';
  }
}
```

### Design System (Netflix-style dark theme)

- Dropdown background: `bg-dark-surface` (`#1a1a1a`)
- Border: `border-dark-border`
- Unread notification: `bg-dark-card/50` with `border-l-2 border-primary`
- Read notification: `bg-transparent`
- Badge: `bg-primary` (`#e50914`) with white text
- Hover: `hover:bg-dark-card`
- Time text: `text-neutral-500 text-xs`
- Body text: `text-neutral-400 text-sm truncate`
- Title text: `text-white text-sm font-medium`

### Edge Cases

- **Dropdown positioning:** Em telas pequenas, o dropdown pode sair da viewport. Usar `right-0` para alinhar à direita.
- **Rapid clicking:** Debounce mark as read calls para evitar chamadas duplicadas.
- **Navigation com dropdown aberto:** Fechar dropdown antes de navegar.
- **Polling em background tab:** `setInterval` continua rodando em tabs inativas — aceitável para v1, otimizar com `document.visibilityState` se necessário.
- **Relative time:** Usar `relative-time.ts` existente no projeto para formatação "há X minutos".

### Testing

- **NotificationBell:** Renderiza bell icon, mostra badge com count, esconde badge quando 0, mostra "9+" para > 9
- **NotificationDropdown:** Renderiza lista de notificações, mostra loading skeleton, mostra empty state, diferencia read/unread styling
- **useNotifications:** Polls unread count, fetches notifications, marks as read, marks all as read
- **Integration:** Click notification → mark read + navigate, click outside → close, Escape → close

## Change Log

| Data       | Versão | Descrição     | Autor  |
|------------|--------|---------------|--------|
| 2026-03-20 | 0.1    | Draft inicial | @sm    |
| 2026-03-20 | 0.2 | Validação PO: aprovada | @po |
| 2026-03-20 | 0.3 | Implementação completa | @dev |

## Dev Agent Record

### Implementation Summary

**Files Created:**
- `src/hooks/useNotifications.ts` — Hook encapsulating notification fetch, polling (30s), markAsRead (with debounce), markAllAsRead, and refresh
- `src/components/member/NotificationBell.tsx` — Bell icon button with unread badge (0 hidden, >9 shows "9+"), click-outside/Escape close, motion/react animated dropdown
- `src/components/member/NotificationDropdown.tsx` — Dropdown panel with notification list, type icons (BookOpen/MessageCircle/Trophy), read/unread styling, skeleton loading, empty state, "Marcar todas como lidas" button, "Ver todas" link
- `tests/unit/components/member/NotificationBell.test.tsx` — 8 tests: render, badge counts, dropdown open/close, Escape, click-outside, polling
- `tests/unit/components/member/NotificationDropdown.test.tsx` — 15 tests: list rendering, loading/empty states, URL generation, read/unread styling, mark-read/mark-all, navigation

**Files Modified:**
- `src/components/member/MemberHeader.tsx` — Added NotificationBell import and placed between search button and user name

**Test Results:** 23/23 passing, 0 failures. Existing MemberHeader tests (4/4) unaffected. TypeScript: 0 errors.

**AC Coverage:**
- AC1: Bell icon in MemberHeader between search and user name (lucide-react Bell)
- AC2: Badge with unread count, hidden at 0, "9+" for >9, red bg-primary
- AC3: Dropdown opens on click with last 10 notifications
- AC4: Type icons, title, body truncated, relative time, read/unread styling with border-l-primary
- AC5: Click marks as read (PATCH), closes dropdown, navigates via Link href
- AC6: "Marcar todas como lidas" calls POST /api/notifications/mark-all-read, resets badge
- AC7: "Ver todas" link to /notifications
- AC8: Polling every 30s via useEffect + setInterval
- AC9: Click-outside (mousedown) and Escape (keydown) close dropdown
- AC10: motion/react AnimatePresence with fade + slide down animation
- AC11: Skeleton loading state in dropdown
- AC12: Empty state "Nenhuma notificação"

## QA Results

**Verdict: PASS**

**Reviewer:** @qa (Quinn)
**Date:** 2026-03-20

### 7-Point Quality Check

| # | Check | Result |
|---|-------|--------|
| 1 | All ACs covered | PASS (12/12 ACs verified) |
| 2 | Code quality | PASS |
| 3 | Test coverage | PASS (23/23 tests, 8 Bell + 15 Dropdown) |
| 4 | TypeScript | PASS (no errors reported) |
| 5 | Naming/conventions | PASS (absolute imports, component patterns consistent) |
| 6 | No regressions | PASS (existing MemberHeader tests 4/4 unaffected) |
| 7 | Story file maintenance | PASS (tasks checked, file list accurate, dev record complete) |

### AC-by-AC Verification

- **AC1** PASS: `NotificationBell` imported and placed in `MemberHeader.tsx` between Search button and user name (line 60). Uses `lucide-react` Bell icon.
- **AC2** PASS: Badge renders with `bg-primary` (red), white text, hidden at count=0, shows "9+" for >9. Test coverage: 3 tests (badge count, 9+, hidden at 0).
- **AC3** PASS: Click opens dropdown via `AnimatePresence`; `refresh()` fetches last 10 notifications (`?limit=10`).
- **AC4** PASS: `NOTIFICATION_ICONS` maps types to correct icons (BookOpen, MessageCircle, Trophy). Read/unread styling with `border-l-primary` for unread, `border-l-transparent` for read. Relative time via `getRelativeTime()`. Body truncated with `truncate` class.
- **AC5** PASS: `handleClickNotification` marks unread as read via `onMarkRead`, closes dropdown via `onClose`. Navigation uses `<Link href={url}>`.
- **AC6** PASS: "Marcar todas como lidas" button calls `onMarkAllRead`. Conditionally shown only when `hasUnread` is true.
- **AC7** PASS: "Ver todas" link in footer points to `/notifications`, calls `onClose` on click.
- **AC8** PASS: `useNotifications` hook polls every 30s via `setInterval(fetchUnreadCount, 30_000)`. Test verifies polling.
- **AC9** PASS: Click-outside (mousedown listener) and Escape (keydown listener) both close dropdown. Both have test coverage.
- **AC10** PASS: `AnimatePresence` + `motion.div` with `initial={{ opacity: 0, y: -8 }}` fade+slide animation from `motion/react`.
- **AC11** PASS: `NotificationItemSkeleton` component renders 3 skeleton rows during loading. Test verifies via `data-testid="notification-skeleton"`.
- **AC12** PASS: Empty state renders "Nenhuma notificação" text. Test verifies.

### Observations

1. **Duplication note:** `NotificationDropdown.tsx` has an inline `getNotificationUrl()` function (lines 16-30) while Story 15.5 extracted a shared version at `lib/notifications/get-notification-url.ts`. The dropdown could import the shared utility instead. Not a blocker -- both implement identical logic.
2. **COMMENT_REPLY navigation data gap (pre-existing, Story 15.3):** The `comment-reply.ts` trigger does not include `productSlug` in notification data, meaning COMMENT_REPLY notifications will fall back to `/` instead of navigating to the lesson. This is a data issue from 15.3 triggers, not from this story's UI implementation. The UI code correctly handles missing data by falling back gracefully. Recommend follow-up fix in 15.3.
3. **Debounce on markAsRead:** Well implemented using `markingRef` with a `Set<string>` to prevent duplicate concurrent calls for the same notification ID.
4. **Accessibility:** Bell button has `aria-label="Notificações"` and meets 44x44 minimum touch target. Good.
