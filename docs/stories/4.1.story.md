# Story 4.1: Webhook Endpoint da Payt

---

## Status

**Ready for Review**

---

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "lint", "build", "manual-review"]

---

## Story

**As a** sistema,
**I want** receber e processar webhooks da Payt,
**so that** compras sejam convertidas automaticamente em acessos.

---

## Acceptance Criteria

1. Route Handler em `/api/webhooks/payt` recebe POST requests da Payt
2. Validação de assinatura/token do webhook para garantir autenticidade (conforme documentação da Payt)
3. Payload parseado para extrair: email do comprador, ID do produto comprado, ID da transação, status da compra
4. Se status = "approved/paid": buscar ou criar membro por email, criar registro em `member_access` vinculando membro ao produto
5. Se membro não existe: criar profile com role=member e enviar email de boas-vindas com credenciais (via Supabase Auth invite)
6. Se acesso já existe para esse membro+produto: ignorar (idempotência)
7. Log de todos os webhooks recebidos para auditoria (tabela `webhook_logs`)
8. Retornar HTTP 200 para webhooks válidos, 401 para inválidos, 500 para erros internos
9. Testes de integração cobrindo cenários: nova compra, membro existente, produto duplicado, assinatura inválida

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1: Criar Route Handler do webhook** (AC: 1, 8)
  - [x] 1.1 Criar `src/app/api/webhooks/payt/route.ts` com handler POST
  - [x] 1.2 Implementar parsing do body JSON com tratamento de erro
  - [x] 1.3 Retornar HTTP 200 para sucesso, 401 para assinatura inválida, 500 para erros internos
  - [x] 1.4 Configurar rota para não requerer autenticação de sessão (rota pública com signature check)

- [x] **Task 2: Implementar validação de assinatura** (AC: 2)
  - [x] 2.1 Criar `src/lib/webhooks/payt-signature.ts` com função `validatePaytSignature(headers, body, secret)`
  - [x] 2.2 Implementar HMAC-SHA256 validation usando `PAYT_WEBHOOK_SECRET` env var
  - [x] 2.3 Rejeitar com 401 se assinatura ausente ou inválida
  - [x] 2.4 Adicionar `PAYT_WEBHOOK_SECRET` ao `.env.example`

- [x] **Task 3: Implementar parsing do payload** (AC: 3)
  - [x] 3.1 Criar `src/types/webhook.ts` com interface `PaytWebhookPayload` (email, product_id, transaction_id, status)
  - [x] 3.2 Validar payload com Zod schema
  - [x] 3.3 Extrair campos relevantes do payload parseado

- [x] **Task 4: Implementar idempotência via transaction_id** (AC: 6)
  - [x] 4.1 Antes de processar, checar se `transaction_id` já existe em `member_access`
  - [x] 4.2 Se existe, retornar 200 (já processado) sem criar duplicata
  - [x] 4.3 Se `member_access` já existe para profile_id+product_id (UNIQUE constraint), ignorar gracefully

- [x] **Task 5: Implementar busca/criação de membro** (AC: 4, 5)
  - [x] 5.1 Criar `src/lib/webhooks/member-provisioning.ts`
  - [x] 5.2 Buscar profile por email via `supabase.auth.admin.listUsers({ email })`
  - [x] 5.3 Se não existe: criar via `supabase.auth.admin.inviteUserByEmail(email, { data: { role: 'member' } })`
  - [x] 5.4 Trigger `handle_new_user()` cria profile automaticamente (já existente na migration)
  - [x] 5.5 Retornar profile_id do membro (existente ou recém-criado)

- [x] **Task 6: Implementar criação de acesso** (AC: 4)
  - [x] 6.1 Buscar `product_id` interno via `product_mappings` usando external_product_id (integração com Story 4.2)
  - [x] 6.2 Inserir registro em `member_access` com: profile_id, product_id, granted_by='webhook', transaction_id
  - [x] 6.3 Tratar constraint violation (UNIQUE) como idempotência — não falhar

- [x] **Task 7: Implementar logging de webhooks** (AC: 7)
  - [x] 7.1 Inserir em `webhook_logs` para todo webhook recebido (antes de processar)
  - [x] 7.2 Atualizar status para 'processed', 'failed' ou 'ignored' após processamento
  - [x] 7.3 Salvar error_message se ocorrer erro
  - [x] 7.4 Salvar payload completo como JSONB para auditoria

- [x] **Task 8: Criar Supabase admin client para webhook** (AC: 4, 5)
  - [x] 8.1 Criar `src/lib/supabase/admin.ts` com client usando `SUPABASE_SERVICE_ROLE_KEY`
  - [x] 8.2 Este client bypassa RLS — usar SOMENTE em contexto de webhook/server-side admin operations
  - [x] 8.3 Documentar com JSDoc que este client tem acesso irrestrito

- [x] **Task 9: Testes de integração** (AC: 9)
  - [x] 9.1 Criar `tests/integration/webhooks/payt-webhook.test.ts`
  - [x] 9.2 Cenário: nova compra — membro novo, acesso criado, log registrado
  - [x] 9.3 Cenário: membro existente — apenas acesso adicionado
  - [x] 9.4 Cenário: compra duplicada (idempotência) — retorna 200, sem duplicata
  - [x] 9.5 Cenário: assinatura inválida — retorna 401
  - [x] 9.6 Cenário: product_id sem mapeamento — retorna 200, log com status 'ignored'
  - [x] 9.7 Cenário: payload inválido — retorna 400

---

## Dev Notes

### Webhook Processing Architecture [Source: architecture.md#7-core-workflows]

O webhook opera fora do contexto de sessão de usuário. Usa `SUPABASE_SERVICE_ROLE_KEY` para bypasear RLS e executar operações admin (criar usuários, inserir acessos).

```
Payt → POST /api/webhooks/payt → Validate Signature → Parse Payload
  → Check Idempotency (transaction_id) → Lookup ProductMapping
  → Find/Create Member → Create MemberAccess → Log → HTTP 200
```

### webhook_logs Schema [Source: architecture.md#8-database-schema]

```sql
CREATE TABLE webhook_logs (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    gateway TEXT NOT NULL DEFAULT 'payt',
    event_type TEXT NOT NULL DEFAULT '',
    payload JSONB NOT NULL DEFAULT '{}',
    status TEXT NOT NULL DEFAULT 'processed' CHECK (status IN ('processed', 'failed', 'ignored')),
    error_message TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_webhook_logs_created ON webhook_logs(created_at DESC);
```

### product_mappings Schema [Source: architecture.md#8-database-schema]

```sql
CREATE TABLE product_mappings (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    external_product_id TEXT NOT NULL,
    product_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,
    gateway TEXT NOT NULL DEFAULT 'payt' CHECK (gateway IN ('payt')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(external_product_id, gateway)
);

CREATE INDEX idx_product_mappings_external ON product_mappings(external_product_id, gateway);
```

### Supabase service_role Usage [Source: architecture.md#16-coding-standards]

```typescript
// src/lib/supabase/admin.ts — WEBHOOK/ADMIN ONLY
import { createClient } from '@supabase/supabase-js';

export function createAdminClient() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } }
  );
}
// WARNING: This client BYPASSES RLS. Only use in webhook handlers
// and server-side admin operations. NEVER expose to client.
```

### Idempotency Pattern [Source: architecture.md#6-external-apis]

```typescript
// Check before processing
const { data: existing } = await adminClient
  .from('member_access')
  .select('id')
  .eq('transaction_id', payload.transaction_id)
  .maybeSingle();

if (existing) {
  // Already processed — return 200, log as 'ignored'
  await logWebhook(payload, 'ignored');
  return NextResponse.json({ status: 'already_processed' }, { status: 200 });
}
```

### Signature Validation Pattern

```typescript
import { createHmac } from 'crypto';

export function validatePaytSignature(
  signature: string | null,
  rawBody: string,
  secret: string
): boolean {
  if (!signature) return false;
  const expected = createHmac('sha256', secret)
    .update(rawBody)
    .digest('hex');
  return signature === expected;
}
```

### Environment Variables Required

```bash
# Add to .env.local
PAYT_WEBHOOK_SECRET=your-payt-webhook-secret
# Already exists:
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### API Spec [Source: architecture.md#5-api-specification]

| Method | Route | Auth | Description |
|--------|-------|------|-------------|
| POST | `/api/webhooks/payt` | Signature | Receber webhook da Payt |

### Error Response Format [Source: architecture.md#17-error-handling-strategy]

```typescript
// 200 — Webhook processed or already processed
// 401 — Invalid signature
// 400 — Invalid payload
// 500 — Internal error (log and alert)
```

### Coding Standards [Source: architecture.md#16-coding-standards]

- **Absolute Imports:** Always use `@/` prefix
- **Webhook Processing:** Always use `supabase.auth.admin` (service role) — never user context
- **Error Handling:** Route Handlers must return `ApiError` format padronizado

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Story criada com contexto completo de webhook, idempotência e service_role | River (SM Agent) |
| 2026-03-11 | 1.1 | Validada e aprovada por PO | Pax (PO Agent) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

- Linter auto-refactored product lookup into separate module `product-lookup.ts`

### Completion Notes List

- HMAC-SHA256 com timingSafeEqual para prevenir timing attacks
- Zod schema valida payload antes de processar
- Idempotência dupla: transaction_id check + UNIQUE constraint (23505)
- Product mapping via `product_mappings` table (external_product_id → internal product_id)
- Member provisioning via `auth.admin.inviteUserByEmail` (trigger cria profile)
- Webhook logging registra antes de processar e atualiza status após
- 12 testes (5 unit signature + 7 integration webhook)

### File List

- `src/lib/supabase/admin.ts` — NEW: Admin client (service_role, bypasses RLS)
- `src/types/webhook.ts` — NEW: PaytWebhookPayload, Zod schema, WebhookLog, ProductMapping types
- `src/lib/webhooks/payt-signature.ts` — NEW: HMAC-SHA256 signature validation
- `src/lib/webhooks/webhook-logger.ts` — NEW: createWebhookLog, updateWebhookLog
- `src/lib/webhooks/member-provisioning.ts` — NEW: findOrCreateMember via auth.admin
- `src/lib/webhooks/product-lookup.ts` — NEW: lookupInternalProduct via product_mappings
- `src/app/api/webhooks/payt/route.ts` — NEW: POST webhook handler
- `supabase/migrations/20260311000009_create_webhook_logs.sql` — NEW: webhook_logs table
- `supabase/migrations/20260311000010_create_product_mappings.sql` — NEW: product_mappings table
- `.env.example` — MODIFIED: Added PAYT_WEBHOOK_SECRET
- `tests/unit/webhooks/payt-signature.test.ts` — NEW: 5 tests
- `tests/integration/webhooks/payt-webhook.test.ts` — NEW: 7 tests

---

## QA Results

_To be filled by QA agent_
