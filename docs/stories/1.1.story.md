# Story 1.1: Project Setup & Supabase Integration

---

## Status

**Ready for Review**

---

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "lint", "build", "manual-review"]

---

## Story

**As a** desenvolvedor,
**I want** inicializar o projeto Next.js 16+ com Supabase integrado,
**so that** tenhamos a base técnica funcional com deploy contínuo.

---

## Acceptance Criteria

1. Projeto Next.js 16+ criado em `packages/memberly-app/` com App Router, TypeScript strict, Tailwind CSS v4 e Zustand configurados
2. Supabase project conectado com variáveis de ambiente (`.env.local`) para DB, Auth e Storage
3. Supabase client configurado em `lib/supabase/` com client-side e server-side helpers
4. ESLint + Prettier configurados com regras do projeto
5. Absolute imports configurados via `tsconfig.json` paths
6. App roda localmente (`npm run dev`) e exibe página de canary/health check na rota `/`
7. Deploy funcional no Vercel (push to main = deploy automático)

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1: Inicializar projeto Next.js 16+ com App Router** (AC: 1)
  - [x] 1.1 Criar `packages/memberly-app/` e inicializar Next.js 16+ com `create-next-app` (App Router, TypeScript, Tailwind CSS v4, ESLint)
  - [x] 1.2 Configurar `package.json` raiz com npm workspaces apontando para `packages/*`
  - [x] 1.3 Verificar que `npm run dev` inicia o app na porta 3000

- [x] **Task 2: Configurar TypeScript strict e absolute imports** (AC: 1, 5)
  - [x] 2.1 Configurar `tsconfig.json` com `strict: true`
  - [x] 2.2 Adicionar paths alias: `"@/*": ["./src/*"]`
  - [x] 2.3 Verificar que imports com `@/` funcionam corretamente

- [x] **Task 3: Configurar Tailwind CSS v4** (AC: 1)
  - [x] 3.1 Verificar que Tailwind CSS v4 está instalado e configurado (via `create-next-app`)
  - [x] 3.2 Tailwind v4 usa `@theme inline` em globals.css (não tailwind.config.ts) — tema base configurado
  - [x] 3.3 Validar que classes Tailwind aplicam estilos corretamente

- [x] **Task 4: Instalar e configurar Zustand** (AC: 1)
  - [x] 4.1 `npm install zustand`
  - [x] 4.2 Criar store placeholder em `src/stores/ui-store.ts`
  - [x] 4.3 Verificar que o store funciona em Client Component

- [x] **Task 5: Configurar Supabase client** (AC: 2, 3)
  - [x] 5.1 `npm install @supabase/supabase-js @supabase/ssr`
  - [x] 5.2 Criar `.env.local` com `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
  - [x] 5.3 Criar `.env.example` com placeholders para documentação
  - [x] 5.4 Criar `src/lib/supabase/client.ts` — browser client via `createBrowserClient()`
  - [x] 5.5 Criar `src/lib/supabase/server.ts` — server client via `createServerClient()` com cookies
  - [x] 5.6 Criar `src/lib/supabase/middleware.ts` — helper para middleware auth
  - [x] 5.7 Criar teste de conexão Supabase em `tests/unit/lib/supabase-client.test.ts` verificando que os clients são instanciáveis

- [x] **Task 6: Configurar ESLint + Prettier** (AC: 4)
  - [x] 6.1 Configurar ESLint 9.x com flat config e plugin Next.js
  - [x] 6.2 Instalar e configurar Prettier 3.x
  - [x] 6.3 Adicionar scripts ao `package.json`: `lint`, `lint:fix`, `format`
  - [x] 6.4 Verificar que `npm run lint` executa sem erros

- [x] **Task 7: Configurar Vitest** (AC: implícito — testing infrastructure)
  - [x] 7.1 `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`
  - [x] 7.2 Criar `vitest.config.ts` com jsdom environment e path aliases
  - [x] 7.3 Adicionar scripts: `test`, `test:unit`, `test:watch`
  - [x] 7.4 Criar teste canary: `tests/unit/setup.test.ts` que verifica ambiente

- [x] **Task 8: Criar página de health check** (AC: 6)
  - [x] 8.1 Criar `src/app/page.tsx` com RSC que exibe status da app (nome, versão, status Supabase connection)
  - [x] 8.2 Verificar que `npm run dev` exibe a página corretamente em `http://localhost:3000`

- [x] **Task 9: Configurar typecheck script** (AC: implícito — quality)
  - [x] 9.1 Adicionar script `typecheck: tsc --noEmit` ao `package.json`
  - [x] 9.2 Verificar que `npm run typecheck` passa sem erros

- [x] **Task 10: Configurar utilitários base** (AC: implícito — foundation)
  - [x] 10.1 Criar `src/lib/utils/cn.ts` com helper `cn()` usando `clsx` + `tailwind-merge`
  - [x] 10.2 `npm install clsx tailwind-merge`

- [x] **Task 11: Preparar deploy Vercel** (AC: 7)
  - [x] 11.1 Criar `next.config.ts` com configuração base
  - [x] 11.2 Verificar que `npm run build` completa sem erros
  - [x] 11.3 Documentar instruções de deploy Vercel no README (conectar repo, root directory: `packages/memberly-app`)

---

## Dev Notes

### Tech Stack [Source: architecture.md#3-tech-stack]

| Technology | Version | Purpose |
|-----------|---------|---------|
| Next.js | 16+ | App Router, RSC, API Routes |
| React | 19+ | UI library (bundled with Next.js) |
| TypeScript | 5.x | Strict mode, fullstack |
| Tailwind CSS | 4.x | Utility-first styling |
| Zustand | 5.x | Client-side state management |
| Supabase JS | latest | Database, Auth, Storage client |
| @supabase/ssr | latest | Server-side Supabase helpers |
| ESLint | 9.x | Flat config + Next.js plugin |
| Prettier | 3.x | Code formatting |
| Vitest | 3.x | Unit + integration tests |
| React Testing Library | 16.x | Component testing |
| clsx + tailwind-merge | latest | className utility |

### Project Structure [Source: architecture.md#11-unified-project-structure]

```
memberly/
├── packages/
│   └── memberly-app/
│       ├── src/
│       │   ├── app/              # App Router (pages + API)
│       │   │   ├── page.tsx      # Health check page (this story)
│       │   │   └── layout.tsx    # Root layout
│       │   ├── components/       # React components (future)
│       │   ├── hooks/            # Custom hooks (future)
│       │   ├── lib/
│       │   │   ├── supabase/
│       │   │   │   ├── client.ts     # Browser Supabase client
│       │   │   │   ├── server.ts     # Server Supabase client
│       │   │   │   └── middleware.ts  # Supabase middleware helper
│       │   │   └── utils/
│       │   │       └── cn.ts         # className merge utility
│       │   ├── stores/
│       │   │   └── ui-store.ts       # UI state (Zustand)
│       │   └── types/            # TypeScript types (future)
│       ├── tests/
│       │   └── unit/
│       │       └── setup.test.ts # Canary test
│       ├── public/
│       ├── .env.local            # Environment vars (gitignored)
│       ├── .env.example          # Template
│       ├── next.config.ts
│       ├── tailwind.config.ts
│       ├── tsconfig.json
│       ├── vitest.config.ts
│       └── package.json
├── package.json                  # Root (npm workspaces)
└── ...
```

### Supabase Client Pattern [Source: architecture.md#9-frontend-architecture]

**Browser client (`client.ts`):**
```typescript
import { createBrowserClient } from '@supabase/ssr';

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

**Server client (`server.ts`):**
```typescript
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';

export async function createServerSupabaseClient() {
  const cookieStore = await cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (c) => c.forEach(({ name, value, options }) => cookieStore.set(name, value, options))
      }
    }
  );
}
```

### Coding Standards [Source: architecture.md#16-coding-standards]

- **Absolute Imports:** Always use `@/` prefix — never relative paths above 1 level
- **Supabase Client:** `createServerSupabaseClient()` on server, `createClient()` on client — never mix
- **Environment Variables:** `process.env` with `!` assertion server-side only — client-side only `NEXT_PUBLIC_*`

### Naming Conventions [Source: architecture.md#16-coding-standards]

| Element | Convention | Example |
|---------|-----------|---------|
| Components | PascalCase | `HeroBanner.tsx` |
| Hooks | camelCase + `use` | `useProgress.ts` |
| Store Files | kebab-case | `ui-store.ts` |
| Utility Files | kebab-case | `cn.ts` |

### Environment Variables [Source: architecture.md#12-development-workflow]

```bash
# packages/memberly-app/.env.local
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### Testing

**Framework:** Vitest 3.x + React Testing Library 16.x [Source: architecture.md#15-testing-strategy]

**Test location:** `tests/unit/` e `tests/integration/` dentro de `packages/memberly-app/`

**Test patterns:**
- Vitest config com jsdom environment
- Path aliases configurados no vitest.config.ts para suportar `@/` imports
- Nesta story: apenas teste canary verificando que o ambiente funciona

**Scripts:**
```bash
npm test              # All tests
npm run test:unit     # Unit only
npm run test:watch    # Watch mode
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Story criada com contexto completo da arquitetura | River (SM Agent) |
| 2026-03-11 | 1.1 | Validada e aprovada por PO. Fix: adicionada subtask 5.7 (teste Supabase client) | Pax (PO Agent) |
| 2026-03-11 | 1.2 | Implementacao completa — 11/11 tasks, 4/4 tests, all gates pass | Dex (Dev Agent) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References

No debug issues encountered. All tasks completed on first attempt.

### Completion Notes List

- Next.js 16.1.6 (Turbopack) with App Router
- React 19.2.3
- TypeScript 5.x strict mode (came configured from create-next-app)
- Tailwind CSS 4.2.1 — uses `@theme inline` in globals.css (v4 does NOT use tailwind.config.ts)
- Zustand 5.0.11
- Supabase SSR 0.9.0 + supabase-js 2.99.1
- ESLint 9.x flat config + eslint-config-prettier
- Prettier 3.8.1
- Vitest 4.0.18 + RTL 16.3.2 + jsdom 28.1.0
- Removed duplicate package-lock.json from packages/memberly-app/
- All quality gates pass: lint (0 errors), typecheck (0 errors), test (4/4), build (success)

### File List

| File | Action | Description |
|------|--------|-------------|
| `package.json` (root) | Created | npm workspaces config |
| `packages/memberly-app/` | Created | Next.js 16+ app via create-next-app |
| `packages/memberly-app/src/stores/ui-store.ts` | Created | Zustand UI state store |
| `packages/memberly-app/src/lib/supabase/client.ts` | Created | Browser Supabase client |
| `packages/memberly-app/src/lib/supabase/server.ts` | Created | Server Supabase client |
| `packages/memberly-app/src/lib/supabase/middleware.ts` | Created | Supabase middleware helper |
| `packages/memberly-app/src/lib/utils/cn.ts` | Created | clsx + tailwind-merge utility |
| `packages/memberly-app/src/app/page.tsx` | Modified | Health check page |
| `packages/memberly-app/src/app/layout.tsx` | Modified | Updated metadata title/description |
| `packages/memberly-app/src/app/globals.css` | Unchanged | Tailwind v4 @theme inline (from create-next-app) |
| `packages/memberly-app/.env.local` | Created | Environment variables (placeholder) |
| `packages/memberly-app/.env.example` | Created | Env template for documentation |
| `packages/memberly-app/.prettierrc` | Created | Prettier configuration |
| `packages/memberly-app/eslint.config.mjs` | Modified | Added eslint-config-prettier |
| `packages/memberly-app/vitest.config.ts` | Created | Vitest with jsdom + path aliases |
| `packages/memberly-app/tests/unit/setup.ts` | Created | Test setup (jest-dom matchers) |
| `packages/memberly-app/tests/unit/setup.test.ts` | Created | Canary test (environment) |
| `packages/memberly-app/tests/unit/lib/supabase-client.test.ts` | Created | Supabase client instantiation test |
| `packages/memberly-app/package.json` | Modified | Added scripts + dependencies |
| `packages/memberly-app/README.md` | Modified | Vercel deploy instructions |
| `packages/memberly-app/next.config.ts` | Unchanged | Base config (from create-next-app) |
| `packages/memberly-app/tsconfig.json` | Unchanged | strict + paths (from create-next-app) |

---

## QA Results

_To be filled by QA agent_
