# Story 2.1: CRUD de Produtos

---

## Status

**Ready for Review**

---

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "lint", "build", "vitest", "manual-review"]

---

## Story

**As a** admin,
**I want** criar, editar, listar e excluir produtos (cursos/mentorias),
**so that** eu possa gerenciar o catálogo de conteúdo oferecido.

---

## Acceptance Criteria

1. Página `/admin/products` lista todos os produtos com: título, banner (thumbnail), status (publicado/rascunho), número de módulos, data de criação
2. Botão "Novo Produto" abre formulário com campos: título, descrição (textarea), banner (upload de imagem), slug (auto-gerado do título, editável)
3. Banner é uploaded para Supabase Storage e URL salva no produto
4. Edição de produto existente com os mesmos campos, pré-preenchidos
5. Toggle de publicação (publicado/rascunho) — produtos não publicados não aparecem para membros
6. Exclusão de produto com confirmação (soft delete ou hard delete com warning)
7. Validação de formulário: título obrigatório, slug único
8. Feedback visual de sucesso/erro nas operações (toast notifications)

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1: TypeScript types para Product** (AC: all)
  - [x] 1.1 Criar/atualizar `src/types/database.ts` com interface `Product` (id, title, description, banner_url, slug, is_published, sort_order, created_at, updated_at)
  - [x] 1.2 Criar `src/types/api.ts` com tipos de request/response para produtos: `CreateProductRequest`, `UpdateProductRequest`, `ProductListResponse`

- [x] **Task 2: API Route Handlers — Products** (AC: 1, 2, 4, 5, 6, 7)
  - [x] 2.1 Criar `src/app/api/products/route.ts` com GET (listar todos com módulo count) e POST (criar produto)
  - [x] 2.2 Criar `src/app/api/products/[id]/route.ts` com GET (detalhes), PATCH (atualizar), DELETE (excluir)
  - [x] 2.3 Criar `src/app/api/products/[id]/publish/route.ts` com PATCH (toggle is_published)
  - [x] 2.4 Implementar validação: título obrigatório, slug único (retornar 409 em duplicata)
  - [x] 2.5 Implementar slug auto-generation a partir do título (slugify) no POST quando slug não fornecido
  - [x] 2.6 Usar `createServerSupabaseClient()` em todos os handlers — admin role enforced via RLS

- [x] **Task 3: Supabase Storage — Banner Upload** (AC: 3)
  - [x] 3.1 Criar bucket `banners` no Supabase Storage (se não existir via migration ou manual)
  - [x] 3.2 Criar `src/app/api/upload/route.ts` — Route Handler para upload de imagem (multipart/form-data), salva no bucket `banners`, retorna URL pública
  - [x] 3.3 Validar tipo de arquivo (image/jpeg, image/png, image/webp) e tamanho máximo (5MB)
  - [x] 3.4 Gerar nome único para arquivo (uuid + extensão original)

- [x] **Task 4: Utility — Slug Generation** (AC: 2, 7)
  - [x] 4.1 Criar `src/lib/utils/slugify.ts` — função que converte título para slug (lowercase, remove acentos, replace spaces com hyphens, remove special chars)
  - [x] 4.2 Adicionar teste unitário em `tests/unit/lib/slugify.test.ts`

- [x] **Task 5: Utility — Toast Notifications** (AC: 8)
  - [x] 5.1 Criar componente `src/components/ui/Toast.tsx` (Client Component) com variantes: success, error, warning
  - [x] 5.2 Criar `src/stores/toast-store.ts` (Zustand) para gerenciar fila de toasts
  - [x] 5.3 Integrar Toast no admin layout (`src/app/admin/layout.tsx`)

- [x] **Task 6: Utility — API Request Helper** (AC: 8)
  - [x] 6.1 Criar `src/lib/utils/api.ts` com `apiRequest<T>()` helper e `ApiRequestError` class
  - [x] 6.2 Criar `src/lib/utils/api-response.ts` com `apiError()` e `apiSuccess()` helpers para Route Handlers

- [x] **Task 7: Admin — Product List Page** (AC: 1)
  - [x] 7.1 Criar `src/app/admin/products/page.tsx` (RSC) — busca produtos via server-side Supabase query
  - [x] 7.2 Criar `src/components/admin/ProductList.tsx` (Client Component) — tabela/grid com colunas: thumbnail, título, status badge, módulos count, data criação
  - [x] 7.3 Criar `src/components/admin/ProductStatusBadge.tsx` — badge visual publicado/rascunho
  - [x] 7.4 Botão "Novo Produto" que navega para `/admin/products/new`
  - [x] 7.5 Estado vazio: mensagem amigável quando não há produtos

- [x] **Task 8: Admin — Product Form (Create/Edit)** (AC: 2, 3, 4, 7)
  - [x] 8.1 Criar `src/app/admin/products/new/page.tsx` — página de criação
  - [x] 8.2 Criar `src/app/admin/products/[id]/page.tsx` — página de edição (RSC carrega dados, passa para form)
  - [x] 8.3 Criar `src/components/admin/ProductForm.tsx` (Client Component) — formulário compartilhado create/edit com: título (input), descrição (textarea), banner (file upload com preview), slug (auto-gerado, editável)
  - [x] 8.4 Criar `src/components/admin/ImageUpload.tsx` (Client Component) — drag-and-drop ou click para upload, preview da imagem, loading state
  - [x] 8.5 Implementar validação client-side: título obrigatório, slug format (lowercase, hyphens only)
  - [x] 8.6 Submit chama API (`POST /api/products` ou `PATCH /api/products/[id]`), mostra toast de sucesso/erro, redireciona para lista

- [x] **Task 9: Admin — Toggle Publicação** (AC: 5)
  - [x] 9.1 Adicionar toggle switch na ProductList para cada produto
  - [x] 9.2 Toggle chama `PATCH /api/products/[id]/publish` com optimistic UI
  - [x] 9.3 Toast de confirmação após toggle

- [x] **Task 10: Admin — Exclusão de Produto** (AC: 6)
  - [x] 10.1 Criar `src/components/ui/ConfirmDialog.tsx` — modal de confirmação reutilizável
  - [x] 10.2 Botão de exclusão na ProductList com ConfirmDialog ("Tem certeza? Esta ação é irreversível.")
  - [x] 10.3 Warning adicional se produto tem módulos vinculados (cascade delete)
  - [x] 10.4 Chama `DELETE /api/products/[id]`, mostra toast, refresh lista

- [x] **Task 11: Testes** (AC: all)
  - [x] 11.1 Testes unitários para `slugify.ts`
  - [x] 11.2 Testes unitários para `api.ts` e `api-response.ts` helpers
  - [x] 11.3 Testes de componentes para `ProductForm.tsx` (validação, submit)
  - [x] 11.4 Testes de integração para Route Handlers (mock Supabase): GET list, POST create, PATCH update, DELETE, toggle publish

---

## Dev Notes

### API Endpoints [Source: architecture.md#5-api-specification]

| Method | Route | Auth | Description |
|--------|-------|------|-------------|
| GET | `/api/products` | Admin | Listar todos os produtos |
| POST | `/api/products` | Admin | Criar produto |
| GET | `/api/products/[id]` | Admin | Detalhes do produto |
| PATCH | `/api/products/[id]` | Admin | Atualizar produto |
| DELETE | `/api/products/[id]` | Admin | Excluir produto |
| PATCH | `/api/products/[id]/publish` | Admin | Toggle publicação |
| PATCH | `/api/products/reorder` | Admin | Reordenar produtos |

### Database Schema [Source: architecture.md#8-database-schema]

```sql
CREATE TABLE products (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    title TEXT NOT NULL,
    description TEXT NOT NULL DEFAULT '',
    banner_url TEXT,
    slug TEXT NOT NULL UNIQUE,
    is_published BOOLEAN NOT NULL DEFAULT false,
    sort_order INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_products_slug ON products(slug);
CREATE INDEX idx_products_published ON products(is_published);
```

**RLS Policies (already created in Story 1.2):**
- `"Admins have full access to products"` — Admin can do ALL on products
- `"Members can view accessible published products"` — Members see only published + with access via member_access

### Data Model [Source: architecture.md#4-data-models]

```typescript
interface Product {
  id: string;              // UUID
  title: string;
  description: string;
  banner_url: string | null;
  slug: string;            // URL-friendly, unique
  is_published: boolean;
  sort_order: number;
  created_at: string;
  updated_at: string;
}
```

### File Paths — New Files to Create

```
packages/memberly-app/src/
├── app/
│   ├── api/
│   │   ├── products/
│   │   │   ├── route.ts                    # GET (list), POST (create)
│   │   │   └── [id]/
│   │   │       ├── route.ts               # GET, PATCH, DELETE
│   │   │       └── publish/route.ts       # PATCH (toggle)
│   │   └── upload/
│   │       └── route.ts                   # POST (file upload)
│   └── admin/
│       └── products/
│           ├── page.tsx                    # Product list page (RSC)
│           ├── new/page.tsx               # Create product page
│           └── [id]/page.tsx              # Edit product page
├── components/
│   ├── ui/
│   │   ├── Toast.tsx                      # Toast notification component
│   │   └── ConfirmDialog.tsx              # Confirmation modal
│   └── admin/
│       ├── ProductList.tsx                # Product table/grid
│       ├── ProductForm.tsx                # Create/edit form
│       ├── ProductStatusBadge.tsx         # Published/draft badge
│       └── ImageUpload.tsx                # Image upload with preview
├── lib/
│   └── utils/
│       ├── slugify.ts                     # Slug generation utility
│       ├── api.ts                         # Frontend API request helper
│       └── api-response.ts                # Backend response helpers
├── stores/
│   └── toast-store.ts                     # Toast state (Zustand)
└── types/
    ├── database.ts                        # DB types (Product, etc.)
    └── api.ts                             # API request/response types
```

### Route Handler Template [Source: architecture.md#10-backend-architecture]

```typescript
// Example: src/app/api/products/route.ts
import { createServerSupabaseClient } from '@/lib/supabase/server';
import { NextResponse, type NextRequest } from 'next/server';

export async function GET() {
  const supabase = await createServerSupabaseClient();

  const { data, error } = await supabase
    .from('products')
    .select('*, modules(count)')
    .order('sort_order');

  if (error) {
    return NextResponse.json(
      { error: { code: 'FETCH_ERROR', message: error.message } },
      { status: 500 }
    );
  }

  return NextResponse.json(data);
}

export async function POST(request: NextRequest) {
  const supabase = await createServerSupabaseClient();
  const body = await request.json();

  if (!body.title) {
    return NextResponse.json(
      { error: { code: 'VALIDATION_ERROR', message: 'Title is required' } },
      { status: 400 }
    );
  }

  const { data, error } = await supabase
    .from('products')
    .insert({ ...body })
    .select()
    .single();

  if (error) {
    return NextResponse.json(
      { error: { code: 'CREATE_ERROR', message: error.message } },
      { status: error.code === '23505' ? 409 : 500 }
    );
  }

  return NextResponse.json(data, { status: 201 });
}
```

### Supabase Storage Upload Pattern

```typescript
// Upload to Supabase Storage
const supabase = await createServerSupabaseClient();
const fileName = `${crypto.randomUUID()}.${ext}`;

const { data, error } = await supabase.storage
  .from('banners')
  .upload(fileName, file, {
    contentType: file.type,
    upsert: false,
  });

// Get public URL
const { data: { publicUrl } } = supabase.storage
  .from('banners')
  .getPublicUrl(data.path);
```

### Error Response Pattern [Source: architecture.md#17-error-handling]

```typescript
// lib/utils/api-response.ts
import { NextResponse } from 'next/server';

export function apiError(code: string, message: string, status: number, details?: Record<string, any>) {
  return NextResponse.json(
    { error: { code, message, ...(details && { details }) } },
    { status }
  );
}

export function apiSuccess<T>(data: T, status = 200) {
  return NextResponse.json(data, { status });
}
```

### Coding Standards [Source: architecture.md#16-coding-standards]

- **Absolute Imports:** Always use `@/` prefix — never relative paths above 1 level
- **Supabase Client:** `createServerSupabaseClient()` on server, `createClient()` on client — never mix
- **Error Handling:** All Route Handlers must return standardized `ApiError` format — never expose raw Supabase errors
- **RLS Trust:** Never duplicate access logic in code — trust Supabase RLS policies
- **Type Generation:** Run `npx supabase gen types ts` after migrations, import from `@/types/database`

### Naming Conventions [Source: architecture.md#16-coding-standards]

| Element | Convention | Example |
|---------|-----------|---------|
| Components | PascalCase | `ProductForm.tsx` |
| Hooks | camelCase + `use` | `useProducts.ts` |
| API Routes | kebab-case dirs | `/api/products/[id]/publish` |
| Store Files | kebab-case | `toast-store.ts` |
| Utility Files | kebab-case | `slugify.ts` |
| Types/Interfaces | PascalCase | `Product`, `CreateProductRequest` |

### Testing

**Framework:** Vitest 3.x + React Testing Library 16.x [Source: architecture.md#15-testing-strategy]

**Test location:** `tests/unit/` and `tests/integration/` inside `packages/memberly-app/`

**Key test files to create:**
- `tests/unit/lib/slugify.test.ts` — slug generation edge cases (accents, special chars, spaces)
- `tests/unit/lib/api.test.ts` — apiRequest helper, error mapping
- `tests/unit/components/admin/ProductForm.test.tsx` — form validation, submit behavior
- `tests/integration/api/products.test.ts` — Route Handler tests with mocked Supabase

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Story criada com contexto completo da arquitetura | River (SM Agent) |
| 2026-03-11 | 1.1 | Validada e aprovada por PO | Pax (PO Agent) |
| 2026-03-11 | 2.0 | Implementação completa — 11 tasks, 32 testes, typecheck + lint OK | Dex (Dev Agent) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References

- Slugify test fix: underscore regex was stripping `_` before replacing with hyphens. Fixed by adding `_` to allowed chars before replacement.
- ESLint fix: replaced `<a>` tags with Next.js `<Link>` in admin layout, products page, and ProductList.
- 2 pre-existing Sidebar.test.tsx failures (backdrop tests) — not related to this story.

### Completion Notes List

- All 11 tasks implemented and tested (32 new tests, all passing)
- TypeScript: 0 errors
- ESLint: 0 errors, 2 warnings (img elements — intentional for dynamic Supabase Storage URLs)
- API follows standardized error format with `apiError()`/`apiSuccess()` helpers
- Slug auto-generation with accent removal (NFD normalization)
- Optimistic UI for publish toggle
- Cascade delete warning when product has modules
- Toast notifications via Zustand store with auto-dismiss (4s)

### File List

**New files:**
- `src/types/api.ts` — API request/response types
- `src/lib/utils/slugify.ts` — Slug generation utility
- `src/lib/utils/api.ts` — Frontend API request helper + ApiRequestError
- `src/lib/utils/api-response.ts` — Backend response helpers (apiError, apiSuccess)
- `src/stores/toast-store.ts` — Toast notification Zustand store
- `src/components/ui/Toast.tsx` — Toast notification component
- `src/components/ui/ConfirmDialog.tsx` — Confirmation modal
- `src/components/admin/ProductStatusBadge.tsx` — Published/draft badge
- `src/components/admin/ImageUpload.tsx` — Image upload with drag-and-drop
- `src/components/admin/ProductForm.tsx` — Product create/edit form
- `src/components/admin/ProductList.tsx` — Product table with actions
- `src/app/api/products/route.ts` — GET (list) + POST (create)
- `src/app/api/products/[id]/route.ts` — GET + PATCH + DELETE
- `src/app/api/products/[id]/publish/route.ts` — PATCH (toggle)
- `src/app/api/upload/route.ts` — POST (file upload to Supabase Storage)
- `src/app/admin/products/page.tsx` — Product list page (RSC)
- `src/app/admin/products/new/page.tsx` — New product page
- `src/app/admin/products/[id]/page.tsx` — Edit product page
- `tests/unit/lib/slugify.test.ts` — 10 tests
- `tests/unit/lib/api.test.ts` — 5 tests
- `tests/unit/lib/api-response.test.ts` — 5 tests
- `tests/unit/components/admin/ProductForm.test.tsx` — 7 tests
- `tests/integration/api/products.test.ts` — 5 tests

**Modified files:**
- `src/types/database.ts` — Added convenience type aliases (Product, Profile, etc.)
- `src/app/admin/layout.tsx` — Integrated ToastContainer

---

## QA Results

_To be filled by QA agent_
