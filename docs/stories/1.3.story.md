# Story 1.3: Auth System (Member + Admin)

---

## Status

**Ready for Review**

---

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "lint", "build", "vitest", "manual-review"]

---

## Dependencies

- **Story 1.1** (Project Setup & Supabase Integration) — Requires Supabase client helpers (`lib/supabase/`), UI utilities (`cn()`), and Zustand configured
- **Story 1.2** (Database Schema & RLS Foundation) — Requires `profiles` table with `role` column for role-based redirect and middleware authorization

---

## Story

**As a** membro ou admin,
**I want** fazer login com email/senha e ser redirecionado para a interface correta,
**so that** eu acesse apenas o que me é permitido.

---

## Acceptance Criteria

1. Página de login (`/login`) com formulário de email/senha usando Supabase Auth
2. Página de recuperação de senha (`/forgot-password`) funcional
3. Após login, redirect automático: `role=admin` → `/admin`, `role=member` → `/` (área de membros)
4. Middleware Next.js protege rotas `/admin/*` (apenas role=admin) e rotas de membro (apenas autenticados)
5. Página de logout funcional que limpa sessão e redireciona ao login
6. Sessão persistida via Supabase Auth (refresh token automático)
7. UI do login é limpa, centralizada, com branding placeholder e responsiva

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1: Criar layout do grupo (auth)** (AC: 7)
  - [x] 1.1 Criar `src/app/(auth)/layout.tsx` — layout centralizado e minimal para páginas de auth (fundo neutro, card centralizado)
  - [x] 1.2 Aplicar estilos Tailwind: `min-h-screen flex items-center justify-center bg-gray-50`
  - [x] 1.3 Incluir branding placeholder (logo/nome "Memberly") no topo do card

- [x] **Task 2: Criar página de login** (AC: 1, 7)
  - [x] 2.1 Criar `src/app/(auth)/login/page.tsx` como Client Component (`'use client'`)
  - [x] 2.2 Formulário com campos: email (type="email", required), senha (type="password", required)
  - [x] 2.3 Botão "Entrar" com loading state
  - [x] 2.4 Link "Esqueci minha senha" apontando para `/forgot-password`
  - [x] 2.5 Chamar `supabase.auth.signInWithPassword({ email, password })` no submit
  - [x] 2.6 Tratar erros: credenciais inválidas → exibir mensagem, erro de rede → exibir mensagem
  - [x] 2.7 Layout responsivo: card de login centralizado, max-width 400px, padding adequado

- [x] **Task 3: Implementar redirect pós-login baseado em role** (AC: 3)
  - [x] 3.1 Após `signInWithPassword` com sucesso, buscar profile do user para obter `role`
  - [x] 3.2 Se `role === 'admin'` → `router.push('/admin')`
  - [x] 3.3 Se `role === 'member'` → `router.push('/')`
  - [x] 3.4 Tratar caso de profile não encontrado (erro gracioso)

- [x] **Task 4: Criar página de recuperação de senha** (AC: 2)
  - [x] 4.1 Criar `src/app/(auth)/forgot-password/page.tsx` como Client Component
  - [x] 4.2 Formulário com campo email e botão "Enviar link de recuperação"
  - [x] 4.3 Chamar `supabase.auth.resetPasswordForEmail(email, { redirectTo })` no submit
  - [x] 4.4 Exibir mensagem de sucesso: "Se o email estiver cadastrado, você receberá um link de recuperação"
  - [x] 4.5 Link "Voltar ao login" apontando para `/login`

- [x] **Task 5: Implementar middleware de autenticação** (AC: 4, 6)
  - [x] 5.1 Criar `src/middleware.ts` na raiz do `src/`
  - [x] 5.2 Configurar Supabase server client no middleware com cookie handling
  - [x] 5.3 Definir rotas públicas: `/login`, `/forgot-password`, `/api/webhooks/*`
  - [x] 5.4 Se usuário não autenticado em rota protegida → redirect para `/login`
  - [x] 5.5 Se rota `/admin/*` e `profile.role !== 'admin'` → redirect para `/`
  - [x] 5.6 Configurar matcher: `['/((?!_next/static|_next/image|favicon.ico).*)']`
  - [x] 5.7 Garantir que o middleware faz refresh do session token (Supabase SSR auto-refresh)

- [x] **Task 6: Implementar funcionalidade de logout** (AC: 5)
  - [x] 6.1 Criar Route Handler `src/app/api/auth/logout/route.ts` que chama `supabase.auth.signOut()`
  - [x] 6.2 Alternativa client-side: criar função `signOut()` que chama `supabase.auth.signOut()` e redireciona para `/login`
  - [x] 6.3 Limpar qualquer state local (Zustand stores) no logout

- [x] **Task 7: Criar Zustand auth store** (AC: 6)
  - [x] 7.1 Criar `src/stores/auth-store.ts` com interface `AuthState`
  - [x] 7.2 Campos: `user: Profile | null`, `isLoading: boolean`, `setUser()`, `clearUser()`
  - [x] 7.3 Criar hook `src/hooks/useAuth.ts` que sincroniza Supabase `onAuthStateChange` com o store

- [x] **Task 8: Criar componentes UI base para auth** (AC: 7)
  - [x] 8.1 Criar `src/components/ui/Button.tsx` — botão reutilizável com variantes (primary, outline) e loading state
  - [x] 8.2 Criar `src/components/ui/Input.tsx` — input reutilizável com label, error message, tipos
  - [x] 8.3 Criar `src/components/ui/Card.tsx` — card container reutilizável
  - [x] 8.4 Todos os componentes usam `cn()` helper para merge de classes Tailwind

- [x] **Task 9: Escrever testes** (AC: implícito — quality)
  - [x] 9.1 Criar `tests/unit/components/LoginForm.test.tsx` — render, validation, submit
  - [x] 9.2 Criar `tests/unit/components/ForgotPasswordForm.test.tsx` — render, submit, success message
  - [x] 9.3 Criar `tests/unit/hooks/useAuth.test.ts` — store updates on auth state change
  - [x] 9.4 Verificar que `npm run lint`, `npm run typecheck`, `npm run build` passam sem erros

---

## Dev Notes

### Route Groups Architecture [Source: architecture.md#9-frontend-architecture]

O Next.js App Router usa route groups para separar layouts:

```
src/app/
├── (auth)/                   # Auth group (public routes)
│   ├── login/page.tsx        # /login
│   ├── forgot-password/page.tsx  # /forgot-password
│   └── layout.tsx            # Centered, minimal layout
├── (member)/                 # Member area (protected, dark theme)
│   ├── page.tsx              # / (home)
│   └── layout.tsx            # Netflix-like layout
├── admin/                    # Admin area (protected, light theme)
│   ├── page.tsx              # /admin (dashboard)
│   └── layout.tsx            # Sidebar layout
└── layout.tsx                # Root layout
```

Route groups `(auth)` and `(member)` use parentheses so they do NOT affect the URL path. The `admin` directory does NOT use parentheses because `/admin` is a real URL segment.

### Middleware Pattern [Source: architecture.md#9-frontend-architecture]

```typescript
// src/middleware.ts
import { createServerClient } from '@supabase/ssr';
import { NextResponse, type NextRequest } from 'next/server';

export async function middleware(request: NextRequest) {
  // Create Supabase client with request/response cookie handling
  let supabaseResponse = NextResponse.next({ request });

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => request.cookies.getAll(),
        setAll: (cookiesToSet) => {
          cookiesToSet.forEach(({ name, value, options }) => {
            request.cookies.set(name, value);
          });
          supabaseResponse = NextResponse.next({ request });
          cookiesToSet.forEach(({ name, value, options }) => {
            supabaseResponse.cookies.set(name, value, options);
          });
        },
      },
    }
  );

  const { data: { user } } = await supabase.auth.getUser();

  const path = request.nextUrl.pathname;

  // Public routes — no auth required
  if (path.startsWith('/login') || path.startsWith('/forgot-password') || path.startsWith('/api/webhooks')) {
    return supabaseResponse;
  }

  // Not authenticated → redirect to login
  if (!user) {
    return NextResponse.redirect(new URL('/login', request.url));
  }

  // Admin routes → require admin role
  if (path.startsWith('/admin')) {
    const { data: profile } = await supabase
      .from('profiles')
      .select('role')
      .eq('id', user.id)
      .single();

    if (profile?.role !== 'admin') {
      return NextResponse.redirect(new URL('/', request.url));
    }
  }

  return supabaseResponse;
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
};
```

**Critical:** The middleware must create a new `NextResponse` and pass cookies from request to response. This is how Supabase SSR refreshes tokens transparently.

### Supabase Auth API [Source: architecture.md#9-frontend-architecture]

**Login (client-side):**
```typescript
import { createClient } from '@/lib/supabase/client';

const supabase = createClient();

// Sign in
const { data, error } = await supabase.auth.signInWithPassword({
  email,
  password,
});

// Get user profile for role-based redirect
const { data: profile } = await supabase
  .from('profiles')
  .select('role')
  .eq('id', data.user.id)
  .single();
```

**Password reset (client-side):**
```typescript
const { error } = await supabase.auth.resetPasswordForEmail(email, {
  redirectTo: `${window.location.origin}/reset-password`,
});
```

**Sign out (client-side):**
```typescript
await supabase.auth.signOut();
```

**Auth state listener:**
```typescript
supabase.auth.onAuthStateChange((event, session) => {
  if (event === 'SIGNED_IN') {
    // Update Zustand store
  } else if (event === 'SIGNED_OUT') {
    // Clear Zustand store
  }
});
```

### Zustand Auth Store Pattern [Source: architecture.md#9-frontend-architecture]

```typescript
// src/stores/auth-store.ts
import { create } from 'zustand';

interface Profile {
  id: string;
  full_name: string;
  avatar_url: string | null;
  role: 'member' | 'admin';
}

interface AuthState {
  user: Profile | null;
  isLoading: boolean;
  setUser: (user: Profile | null) => void;
  clearUser: () => void;
  setLoading: (loading: boolean) => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  isLoading: true,
  setUser: (user) => set({ user, isLoading: false }),
  clearUser: () => set({ user: null, isLoading: false }),
  setLoading: (isLoading) => set({ isLoading }),
}));
```

### UI Component Conventions [Source: architecture.md#16-coding-standards]

- **Components:** PascalCase files (`Button.tsx`, `Input.tsx`, `Card.tsx`)
- **Location:** `src/components/ui/` for base reusable components
- **Styling:** Tailwind CSS classes via `cn()` helper from `@/lib/utils/cn`
- **Props pattern:** Use `React.ComponentPropsWithoutRef<'element'>` for native props extension

```typescript
// Example: src/components/ui/Button.tsx
import { cn } from '@/lib/utils/cn';

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'outline';
  isLoading?: boolean;
}

export function Button({ className, variant = 'primary', isLoading, children, disabled, ...props }: ButtonProps) {
  return (
    <button
      className={cn(
        'px-4 py-2 rounded-lg font-medium transition-colors focus:outline-none focus:ring-2',
        variant === 'primary' && 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500',
        variant === 'outline' && 'border border-gray-300 text-gray-700 hover:bg-gray-50 focus:ring-gray-500',
        (disabled || isLoading) && 'opacity-50 cursor-not-allowed',
        className
      )}
      disabled={disabled || isLoading}
      {...props}
    >
      {isLoading ? 'Carregando...' : children}
    </button>
  );
}
```

### File Paths Summary

| File | Purpose |
|------|---------|
| `src/app/(auth)/layout.tsx` | Auth layout (centered, minimal) |
| `src/app/(auth)/login/page.tsx` | Login page |
| `src/app/(auth)/forgot-password/page.tsx` | Password recovery page |
| `src/middleware.ts` | Auth + role routing middleware |
| `src/app/api/auth/logout/route.ts` | Logout route handler |
| `src/stores/auth-store.ts` | Zustand auth state |
| `src/hooks/useAuth.ts` | Auth state sync hook |
| `src/components/ui/Button.tsx` | Button component |
| `src/components/ui/Input.tsx` | Input component |
| `src/components/ui/Card.tsx` | Card component |

### Coding Standards [Source: architecture.md#16-coding-standards]

- **Absolute Imports:** Always use `@/` prefix — `@/lib/supabase/client`, `@/components/ui/Button`
- **Supabase Client:** `createClient()` on client-side (login page), `createServerSupabaseClient()` on server-side (middleware, route handlers)
- **Error Handling:** Return `ApiError` format from route handlers, show user-friendly messages in UI
- **Environment Variables:** Only `NEXT_PUBLIC_*` in client components

### Testing

**Framework:** Vitest 3.x + React Testing Library 16.x [Source: architecture.md#15-testing-strategy]

**Test location:** `packages/memberly-app/tests/unit/`

**Test patterns for this story:**
- Component tests: render login form, simulate submit, verify error states
- Hook tests: mock Supabase auth, verify store updates
- Mock Supabase client using `vi.mock('@/lib/supabase/client')`

**Scripts:**
```bash
npm test              # All tests
npm run test:unit     # Unit only
npm run lint          # ESLint
npm run typecheck     # tsc --noEmit
npm run build         # Next.js build
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Story criada com middleware pattern, auth flows e componentes UI detalhados | River (SM Agent) |
| 2026-03-11 | 1.1 | Validada e aprovada por PO. Fix: adicionada seção Dependencies (Stories 1.1, 1.2) | Pax (PO Agent) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (Dex — YOLO mode)

### Debug Log References

- Lint warning: `vi` unused import in `useAuth.test.ts` — removed
- Pre-existing lint warning: `@next/next/no-img-element` in `ImageUpload.tsx` (not from this story)

### Completion Notes List

- All 9 tasks completed with full coverage
- Auth layout with "Memberly" branding placeholder
- Login page with email/password, error handling, loading state
- Role-based redirect: admin → `/admin`, member → `/`
- Forgot password with success message flow
- Middleware with public routes, auth check, and admin role verification
- Logout via POST route handler (`/api/auth/logout`)
- Zustand auth store + `useAuth` hook with `onAuthStateChange` sync
- 3 UI components: Button (primary/outline + loading), Input (label + error), Card
- 18 tests passing (6 LoginForm, 4 ForgotPassword, 4 useAuth store, 3 setup canary, 1 supabase client)
- Lint: 0 errors, typecheck: clean

### File List

| File | Action | Description |
|------|--------|-------------|
| `src/app/(auth)/layout.tsx` | Created | Auth layout — centered, minimal, Memberly branding |
| `src/app/(auth)/login/page.tsx` | Created | Login page with email/password form + role redirect |
| `src/app/(auth)/forgot-password/page.tsx` | Created | Password recovery page with success state |
| `src/middleware.ts` | Created | Auth middleware — public routes, login check, admin role gate |
| `src/app/api/auth/logout/route.ts` | Created | Logout route handler (POST, signOut + redirect) |
| `src/stores/auth-store.ts` | Created | Zustand auth store (user, isLoading, setUser, clearUser) |
| `src/hooks/useAuth.ts` | Created | Auth hook — syncs Supabase onAuthStateChange with store |
| `src/components/ui/Button.tsx` | Created | Reusable button (primary/outline variants, loading state) |
| `src/components/ui/Input.tsx` | Created | Reusable input (label, error message) |
| `src/components/ui/Card.tsx` | Created | Reusable card container |
| `tests/unit/components/LoginForm.test.tsx` | Created | 6 tests: render, links, errors, redirects, loading |
| `tests/unit/components/ForgotPasswordForm.test.tsx` | Created | 4 tests: render, links, success, error |
| `tests/unit/hooks/useAuth.test.ts` | Created | 4 tests: initial state, setUser, clearUser, setLoading |

---

## QA Results

_To be filled by QA agent_
