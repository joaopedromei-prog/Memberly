# Story 14.5 — Validação Pública de Certificado via Hash

> **Epic 14 — Certificados de Conclusão**

## Status: Ready for Review

## Executor Assignment

| Metadata        | Value                          |
|-----------------|--------------------------------|
| Work Type       | Code / UI / API / Public Page  |
| Primary Executor| @dev                           |
| Quality Gate    | @qa                            |
| Estimated Effort| ~1 hora                        |
| Risk            | Low (página pública simples)   |

## Story

**Como** qualquer pessoa com o link de validação de um certificado,
**Quero** acessar uma página pública que exibe os dados do certificado,
**Para que** eu possa verificar a autenticidade do certificado de conclusão de um membro.

## Acceptance Criteria

1. **AC1** — Página pública acessível em `/certificates/[hash]` sem autenticação (rota pública).
2. **AC2** — A página consulta a tabela `certificates` pelo `hash` e exibe: nome do membro, título do produto, data de emissão e status "Certificado Válido".
3. **AC3** — Se o hash não for encontrado, exibe página com mensagem "Certificado não encontrado" e status "Inválido".
4. **AC4** — A página não requer login — é 100% pública para permitir compartilhamento e validação externa.
5. **AC5** — O endpoint `GET /api/certificates/validate/[hash]` retorna `{ valid: true, certificate: { memberName, productTitle, issuedAt } }` ou `{ valid: false }`.
6. **AC6** — A rota `/certificates` é adicionada ao `PUBLIC_ROUTES` no middleware para bypass de autenticação.
7. **AC7** — A página segue o design system Netflix-style (dark theme) com badge visual de "Certificado Válido" ou "Certificado Inválido".
8. **AC8** — A página inclui link para download do PDF se o certificado for válido.

## Tasks / Subtasks

- [x] **T1** — Adicionar `/certificates` ao array `PUBLIC_ROUTES` em `src/middleware.ts` → (AC4, AC6)
- [x] **T2** — Criar route handler `src/app/api/certificates/validate/[hash]/route.ts` com GET handler que busca certificado por hash com dados de profile e product → (AC5)
- [x] **T3** — Criar página `src/app/certificates/[hash]/page.tsx` como server component que renderiza dados do certificado → (AC1, AC2, AC3, AC7)
- [x] **T4** — Criar layout `src/app/certificates/layout.tsx` com design standalone (sem header/sidebar de membro) → (AC7)
- [x] **T5** — Adicionar link de download do PDF na página de validação → (AC8)
- [x] **T6** — Testes unitários para a página de validação (certificado válido, inválido) → (AC2, AC3)
- [x] **T7** — Testes de integração para `GET /api/certificates/validate/[hash]` → (AC5)

## Dev Notes

### Relevant Source Tree

```
packages/memberly-app/
├── src/
│   ├── app/
│   │   ├── certificates/
│   │   │   ├── layout.tsx             # (NOVO) Layout standalone (sem auth UI)
│   │   │   └── [hash]/
│   │   │       └── page.tsx           # (NOVO) Página pública de validação
│   │   └── api/certificates/
│   │       ├── generate/route.ts      # (Story 14.2)
│   │       └── validate/
│   │           └── [hash]/
│   │               └── route.ts       # (NOVO) GET validation endpoint
│   ├── middleware.ts                   # Adicionar /certificates ao PUBLIC_ROUTES
│   └── lib/supabase/
│       └── admin.ts                   # Service role client para query pública
└── tests/
    ├── unit/pages/certificates/       # (NOVO)
    └── integration/api/certificates/  # (NOVO)
```

### Middleware Update

```typescript
// src/middleware.ts
const PUBLIC_ROUTES = ['/login', '/forgot-password', '/api/webhooks', '/certificates'];
```

### API Route — Validation Endpoint

```typescript
// src/app/api/certificates/validate/[hash]/route.ts
import { createAdminClient } from '@/lib/supabase/admin';
import { apiError, apiSuccess } from '@/lib/utils/api-response';

export async function GET(
  _request: NextRequest,
  { params }: { params: Promise<{ hash: string }> }
) {
  const { hash } = await params;
  const supabase = createAdminClient();

  const { data: certificate } = await supabase
    .from('certificates')
    .select('id, hash, issued_at, certificate_url, profiles(full_name), products(title)')
    .eq('hash', hash)
    .maybeSingle();

  if (!certificate) {
    return apiSuccess({ valid: false });
  }

  return apiSuccess({
    valid: true,
    certificate: {
      memberName: certificate.profiles?.full_name ?? 'Membro',
      productTitle: certificate.products?.title ?? 'Produto',
      issuedAt: certificate.issued_at,
      certificateUrl: certificate.certificate_url,
    },
  });
}
```

### Public Page — Server Component

A página deve usar `createAdminClient()` (service role) para query direta sem autenticação, pois é uma rota pública e o Supabase server client (cookie-based) não terá sessão.

```typescript
// src/app/certificates/[hash]/page.tsx
import { createAdminClient } from '@/lib/supabase/admin';

export default async function CertificateValidationPage({
  params,
}: {
  params: Promise<{ hash: string }>;
}) {
  const { hash } = await params;
  const supabase = createAdminClient();

  const { data: certificate } = await supabase
    .from('certificates')
    .select('id, hash, issued_at, certificate_url, profiles(full_name), products(title)')
    .eq('hash', hash)
    .maybeSingle();

  if (!certificate) {
    return <InvalidCertificate />;
  }

  return <ValidCertificate certificate={certificate} />;
}
```

### Page Design — Netflix-style

**Válido:**
- Background: `#141414`
- Card central com borda `#e50914` (primary)
- Badge verde "Certificado Válido" com ícone de check
- Nome do membro, produto, data em tipografia clara
- Botão "Baixar PDF" com link ao `certificate_url`

**Inválido:**
- Mesmo layout, mas badge vermelha "Certificado Não Encontrado"
- Mensagem: "O certificado com este código de validação não foi encontrado."

### Layout Standalone

A página de validação NÃO deve usar o layout de membro (`(member)/layout.tsx`) nem o layout admin. Criar layout próprio minimalista:

```tsx
// src/app/certificates/layout.tsx
export default function CertificatesLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen bg-dark-bg flex items-center justify-center p-4">
      {children}
    </div>
  );
}
```

### Edge Cases

- **Hash inexistente:** Exibir página de "não encontrado" sem revelar informação sobre hashes válidos.
- **Certificate_url nulo:** Se o PDF ainda não foi uploaded (edge case improvável), não exibir botão de download.
- **SEO:** Adicionar `metadata` com `title` dinâmico para compartilhamento (ex: "Certificado de [Nome] - Memberly").
- **Rate limiting:** Considerar rate limit futuro para evitar enumeração de hashes (fora do escopo desta story).

### Testing

- **Unit:** Renderizar página com certificado válido → exibe nome, produto, data, badge verde
- **Unit:** Renderizar página com hash inválido → exibe mensagem de não encontrado, badge vermelha
- **Integration:** GET `/api/certificates/validate/[hash-valido]` → `{ valid: true, certificate: {...} }`
- **Integration:** GET `/api/certificates/validate/[hash-inexistente]` → `{ valid: false }`
- Verificar que `/certificates/abc123` é acessível sem autenticação (middleware bypass)

## Change Log

| Data       | Versão | Descrição     | Autor  |
|------------|--------|---------------|--------|
| 2026-03-20 | 0.1    | Draft inicial | @sm    |
| 2026-03-20 | 0.2 | Validação PO: aprovada | @po |
| 2026-03-20 | 1.0 | Implementation complete | @dev |

## Dev Agent Record

**Implementation completed by @dev (Dex)**

All 7 tasks implemented. TypeScript typecheck passes. All 6 tests (3 unit + 3 integration) pass.

### File List

| File | Action |
|------|--------|
| `src/middleware.ts` | Modified — added `/certificates` to `PUBLIC_ROUTES` |
| `src/app/api/certificates/validate/[hash]/route.ts` | Created — GET endpoint returning `{ valid, certificate }` |
| `src/app/certificates/layout.tsx` | Created — standalone layout with dark theme, centered |
| `src/app/certificates/[hash]/page.tsx` | Created — server component with valid/invalid states, PDF download, dynamic metadata |
| `tests/unit/pages/certificates/certificate-validation.test.ts` | Created — 3 unit tests (valid cert, invalid cert, no PDF URL) |
| `tests/integration/api/certificates/validate.test.ts` | Created — 3 integration tests (valid hash, invalid hash, null profile/product fallbacks) |

### Implementation Notes

- Used `createAdminClient()` (service role) for both API route and page since these are public routes without user session.
- FK join types from Supabase (`profiles(full_name)`, `products(title)`) required explicit type casting due to TypeScript inferring arrays instead of single objects.
- Page includes `generateMetadata()` for dynamic SEO titles when sharing certificate links.
- Download button conditionally rendered only when `certificate_url` is non-null.
- Invalid hash shows a friendly "not found" page (not a 404) to avoid information leakage about valid hashes.

## QA Results

### Gate Decision: PASS
**Reviewed by:** @qa (Quinn) — 2026-03-20
**AC Coverage:** 8/8 (100%)

- **AC1** — PASS: Page at `src/app/certificates/[hash]/page.tsx` is a server component. Route is public (no auth layout, standalone layout).
- **AC2** — PASS: Valid certificate page displays member name (`data-testid="member-name"`), product title (`data-testid="product-title"`), issued date (`data-testid="issued-at"`), and "Certificado Valido" badge (`data-testid="valid-badge"`). Uses FK joins `profiles(full_name)` and `products(title)`.
- **AC3** — PASS: Invalid hash renders `InvalidCertificate` component with "Certificado Nao Encontrado" badge and friendly message. No information leakage about valid hashes (uses `maybeSingle` returning null, not 404).
- **AC4** — PASS: `/certificates` added to `PUBLIC_ROUTES` in `middleware.ts` line 4. Uses `path.startsWith('/certificates')` matching. No auth required.
- **AC5** — PASS: `GET /api/certificates/validate/[hash]` returns `{ valid: true, certificate: { memberName, productTitle, issuedAt, certificateUrl } }` or `{ valid: false }`. Uses `createAdminClient()` for public access.
- **AC6** — PASS: `/certificates` is in the `PUBLIC_ROUTES` array in middleware. Confirmed via code review.
- **AC7** — PASS: Dark theme with `bg-dark-surface`, `bg-dark-card`, green badge for valid, red badge for invalid. Uses lucide-react icons (ShieldCheck, CheckCircle2, XCircle, Download). Netflix-style consistent.
- **AC8** — PASS: Download button conditionally rendered when `certificate.certificate_url` is non-null. Uses `<a href>` with `target="_blank"` and `rel="noopener noreferrer"`. Test covers null URL case.

**Issues:** None.

**Observações:**
- Uses `createAdminClient()` (service role) correctly for both API route and page since there is no user session on public routes.
- `generateMetadata()` provides dynamic SEO titles for shared certificate links — good addition.
- FK join type casting (`as unknown as { full_name: string } | null`) is necessary due to Supabase TypeScript inference limitations — acceptable pattern.
- Tests: 3 unit (valid, invalid, null URL) + 3 integration (valid hash, invalid hash, null profile/product fallbacks) = 6 tests. Adequate for the scope.
- Layout is standalone (`certificates/layout.tsx`) with centered content and dark background. Clean separation from member/admin layouts.
