# Story 11.3: Expandir Button.tsx com Variantes Completas

---

## Status

**Approved**

---

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "lint", "build", "vitest", "manual-review"]

---

## Story

**As a** developer,
**I want** que o componente `Button.tsx` tenha variantes para todos os padrões de botão usados no projeto,
**so that** botões inline com estilos hardcoded possam ser substituídos pelo componente, garantindo consistência visual.

---

## Acceptance Criteria

- [ ] `Button.tsx` atualizado com variantes: `primary`, `outline`, `ghost`, `danger`, `text`, `icon`
- [ ] Prop `size` adicionada: `sm` (h-8 px-3 text-xs), `md` (h-10 px-4 text-sm), `lg` (h-11 px-6 text-base)
- [ ] Variante `icon` renderiza botão quadrado com tamanho baseado em `size`
- [ ] Testes unitários para todas as variantes e tamanhos
- [ ] Botões de ação em `ProductList.tsx` (edit, delete, copy) migrados para `<Button variant="icon">`
- [ ] Botões de ação em `ModuleList.tsx` migrados
- [ ] Botões de ação em `LessonList.tsx` migrados
- [ ] Botões danger (deletar) migrados para `<Button variant="danger">`
- [ ] Botões ghost (toolbar, nav) migrados para `<Button variant="ghost">`
- [ ] Backward-compatible — usos existentes de `primary` e `outline` não quebram
- [ ] `npm run typecheck` passa sem erros
- [ ] `npm run build` compila sem erros

---

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

---

## Tasks / Subtasks

- [ ] **Task 1: Expandir `Button.tsx` com variantes e sizes** (AC: 1-3)
  - [ ] Adicionar variantes: `ghost`, `danger`, `text`, `icon`
  - [ ] Adicionar prop `size`: `sm`, `md` (default), `lg`
  - [ ] Variante `icon`: `aspect-square`, centralizado, sem texto padding
  - [ ] Variante `ghost`: `bg-transparent hover:bg-gray-100 text-gray-700`
  - [ ] Variante `danger`: `bg-red-600 hover:bg-red-700 text-white`
  - [ ] Variante `text`: `bg-transparent text-blue-600 hover:text-blue-800` sem border
  - [ ] Manter `isLoading` funcional em todas as variantes
- [ ] **Task 2: Criar testes unitários** (AC: 4)
  - [ ] Criar `tests/unit/components/ui/Button.test.tsx`
  - [ ] Testar renderização de cada variante
  - [ ] Testar renderização de cada size
  - [ ] Testar combinações variant+size
  - [ ] Testar isLoading state
  - [ ] Testar backward-compatibility (default = primary + md)
- [ ] **Task 3: Migrar botões icon em Lists** (AC: 5-7)
  - [ ] `ProductList.tsx` — botões edit/delete/copy → `<Button variant="icon" size="sm">`
  - [ ] `ModuleList.tsx` — botões de ação → `<Button variant="icon" size="sm">`
  - [ ] `LessonList.tsx` — botões de ação → `<Button variant="icon" size="sm">`
- [ ] **Task 4: Migrar botões danger e ghost** (AC: 8-9)
  - [ ] Identificar botões de delete com `bg-red-*` ou `text-red-*` → `<Button variant="danger">`
  - [ ] `AdminHeader.tsx` — hamburger menu → `<Button variant="ghost">`
  - [ ] `RichTextEditor.tsx` — toolbar buttons → `<Button variant="ghost" size="sm">`
- [ ] **Task 5: Validar backward-compatibility e build** (AC: 10-12)
  - [ ] Verificar que `<Button>` sem variant ainda renderiza como primary
  - [ ] Verificar que `<Button variant="outline">` ainda funciona
  - [ ] Executar `npm run typecheck`
  - [ ] Executar `npm run build`
  - [ ] Executar testes: `npx vitest run tests/unit/components/ui/Button.test.tsx`

---

## Dev Notes

### Context

O `Button.tsx` atual suporta apenas 2 variantes (`primary` e `outline`), mas o codebase usa 7 padrões distintos de botão. ~120 botões HTML nativos com estilos inline estão espalhados pelo codebase.

### API Proposta

```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'outline' | 'ghost' | 'danger' | 'text' | 'icon';
  size?: 'sm' | 'md' | 'lg';
  isLoading?: boolean;
}
```

### Decisões Técnicas

- Usar approach de `variant` + `size` (padrão shadcn/ui)
- Variante `icon` é quadrada (`aspect-square`) com padding centralizado
- Variante `danger` segue mesma estrutura que `primary` mas com cores red
- Variante `text` não tem background nem border, apenas cor de texto
- Variante `ghost` tem background transparente com hover sutil
- O componente continua estendendo `React.ButtonHTMLAttributes`
- Não migrar TODOS os 120 botões nesta story — apenas os padrões mais comuns nos Lists e toolbar

### Mapeamento de Estilos Atuais → Variantes

| Estilo Inline Atual | Nova Variante |
|---------------------|---------------|
| `bg-blue-600 hover:bg-blue-700 text-white` | `variant="primary"` (já existe) |
| `border border-gray-300 text-gray-700 hover:bg-gray-50` | `variant="outline"` (já existe) |
| `w-8 h-8 rounded-md text-blue-600 hover:bg-blue-50` | `variant="icon" size="sm"` |
| `text-sm text-blue-600 hover:text-blue-800 font-medium` | `variant="text"` |
| `rounded-md p-2 text-gray-500 hover:bg-gray-100` | `variant="ghost"` |
| `bg-red-600 hover:bg-red-700 text-white` | `variant="danger"` |
| `h-11 px-6 bg-primary text-white font-semibold` | `variant="primary" size="lg"` |

### Testing

- **Framework:** vitest + @testing-library/react + jsdom
- **Localização:** `packages/memberly-app/tests/unit/components/ui/Button.test.tsx` (novo)
- **Setup:** `tests/unit/setup.ts` (já existente)
- **Cobertura necessária:**
  - Cada variante renderiza com classes CSS corretas
  - Cada size renderiza com dimensões corretas
  - `isLoading` mostra texto "Carregando..." e desabilita
  - Default (sem props) = primary + md
  - `className` custom é mergeado (não sobrescreve)

### Referências

- Componente atual: `src/components/ui/Button.tsx`
- Padrão shadcn/ui Button: variante + size approach
- Auditoria de Design System: conversa com @ux-design-expert (2026-03-13)

---

## File List

- [ ] `src/components/ui/Button.tsx`
- [ ] `tests/unit/components/ui/Button.test.tsx` (new)
- [ ] `src/components/admin/ProductList.tsx`
- [ ] `src/components/admin/ModuleList.tsx`
- [ ] `src/components/admin/LessonList.tsx`
- [ ] `src/components/admin/AdminHeader.tsx`
- [ ] `src/components/ui/RichTextEditor.tsx`

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-13 | 1.0 | Story criada a partir da auditoria de design system | @ux-design-expert |
| 2026-03-13 | 1.1 | Validação PO: adicionadas Tasks, Dev Notes, Testing, mapeamento de estilos | @po |

---

## Estimation

**Effort:** ~3-4 horas
**Risk:** Médio — mudança no componente base requer atenção para não quebrar usos existentes
**Priority:** Alta — reduz ~120 botões inline para usos do componente centralizado
