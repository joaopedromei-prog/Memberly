# Story 4.2: Mapeamento Produto Payt → Produto Memberly

---

## Status

**Ready for Review**

---

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "lint", "build", "manual-review"]

---

## Story

**As a** admin,
**I want** mapear IDs de produtos da Payt para produtos do Memberly,
**so that** o webhook saiba qual produto liberar.

---

## Acceptance Criteria

1. Tabela `product_mappings` com: `id`, `external_product_id` (ID na Payt), `product_id` (FK para products), `gateway` (payt), `created_at`
2. Na edição de produto no admin, campo para informar o "ID externo (Payt)" do produto
3. Webhook usa `product_mappings` para traduzir o ID do produto recebido no payload para o produto interno
4. Se ID externo não encontrado no mapeamento: logar warning e retornar 200 (não falhar)
5. Um produto pode ter múltiplos mapeamentos (diferentes ofertas na Payt para o mesmo produto)

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1: Verificar migration da tabela product_mappings** (AC: 1)
  - [x] 1.1 Confirmar que a migration de `product_mappings` já existe (criada na Story 1.2)
  - [x] 1.2 Se não existir, criar migration com schema: `id`, `external_product_id`, `product_id` (FK), `gateway`, `created_at`, UNIQUE(external_product_id, gateway)
  - [x] 1.3 Verificar que RLS está habilitada com policy admin-only
  - [x] 1.4 Verificar index `idx_product_mappings_external` em (external_product_id, gateway)

- [x] **Task 2: Criar tipo TypeScript para ProductMapping** (AC: 1)
  - [x] 2.1 Adicionar interface `ProductMapping` em `src/types/database.ts` ou type generation
  - [x] 2.2 Incluir: id, external_product_id, product_id, gateway, created_at

- [x] **Task 3: Adicionar campo de mapeamento na edição de produto** (AC: 2, 5)
  - [x] 3.1 Na página de edição de produto (`/admin/products/[id]`), adicionar seção "Integrações Externas"
  - [x] 3.2 Campo de texto "ID Externo (Payt)" com placeholder explicativo
  - [x] 3.3 Suportar múltiplos mapeamentos: botão "Adicionar outro ID" para diferentes ofertas
  - [x] 3.4 Listar mapeamentos existentes com botão de remover
  - [x] 3.5 Salvar mapeamentos via API ao salvar o produto

- [x] **Task 4: Criar API para gerenciar product_mappings** (AC: 2, 5)
  - [x] 4.1 Criar `POST /api/products/[id]/mappings` — adicionar mapeamento
  - [x] 4.2 Criar `GET /api/products/[id]/mappings` — listar mapeamentos do produto
  - [x] 4.3 Criar `DELETE /api/products/[id]/mappings/[mappingId]` — remover mapeamento
  - [x] 4.4 Validação: external_product_id não pode ser vazio, gateway deve ser 'payt'
  - [x] 4.5 Tratar UNIQUE constraint violation (mesmo external_id + gateway já mapeado)

- [x] **Task 5: Implementar lookup no webhook handler** (AC: 3, 4)
  - [x] 5.1 Criar `src/lib/webhooks/product-lookup.ts` com função `lookupInternalProduct(externalId, gateway)`
  - [x] 5.2 Query: `SELECT product_id FROM product_mappings WHERE external_product_id = $1 AND gateway = $2`
  - [x] 5.3 Se não encontrado: logar warning em `webhook_logs` com status 'ignored', retornar null
  - [x] 5.4 Se encontrado: retornar product_id interno
  - [x] 5.5 Integrar no handler de webhook (Story 4.1, Task 6)

- [x] **Task 6: Testes** (AC: 1-5)
  - [x] 6.1 Criar `tests/unit/lib/webhooks/product-lookup.test.ts` — lookup com/sem mapeamento
  - [x] 6.2 Criar `tests/integration/admin/product-mappings.test.ts` — CRUD de mapeamentos
  - [x] 6.3 Testar: múltiplos mapeamentos para mesmo produto, constraint violation, lookup miss

---

## Dev Notes

### product_mappings Schema [Source: architecture.md#8-database-schema]

```sql
CREATE TABLE product_mappings (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    external_product_id TEXT NOT NULL,
    product_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,
    gateway TEXT NOT NULL DEFAULT 'payt' CHECK (gateway IN ('payt')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(external_product_id, gateway)
);

CREATE INDEX idx_product_mappings_external ON product_mappings(external_product_id, gateway);
```

### Supabase service_role Usage for Webhook [Source: architecture.md#16-coding-standards]

O lookup de product_mappings no contexto do webhook usa o admin client (service_role) porque o webhook não tem sessão de usuário. Isso bypassa RLS.

```typescript
// In webhook handler — uses admin client
const adminClient = createAdminClient();
const { data: mapping } = await adminClient
  .from('product_mappings')
  .select('product_id')
  .eq('external_product_id', externalProductId)
  .eq('gateway', 'payt')
  .maybeSingle();
```

### Admin UI Integration

O campo de mapeamento é adicionado à página de edição de produto existente (Epic 2, Story 2.1). A UI deve se integrar ao formulário existente sem quebrar funcionalidade.

```
/admin/products/[id] (edit page)
├── Campos existentes: título, descrição, banner, slug, publicação
└── Nova seção: "Integrações Externas"
    ├── Label: "IDs de Produto na Payt"
    ├── Lista de mapeamentos existentes (com botão remover)
    └── Botão "Adicionar ID Externo"
```

### Múltiplos Mapeamentos

Um produto pode ter vários IDs externos (ofertas diferentes na Payt apontando para o mesmo produto no Memberly). Exemplo: oferta anual e oferta mensal mapeiam para o mesmo curso.

### RLS Policy [Source: architecture.md#8-database-schema]

```sql
-- product_mappings: admin only
ALTER TABLE product_mappings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Admins have full access to product_mappings"
    ON product_mappings FOR ALL
    USING (
        EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin')
    );
```

### Coding Standards [Source: architecture.md#16-coding-standards]

- **Absolute Imports:** Always use `@/` prefix
- **Supabase Client:** `createServerSupabaseClient()` for admin API routes, `createAdminClient()` for webhook context
- **Error Handling:** Return `ApiError` format on validation/constraint errors

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Story criada com contexto de mapeamento, schema e integração com webhook | River (SM Agent) |
| 2026-03-11 | 1.1 | Validada e aprovada por PO | Pax (PO Agent) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References

- Migration `20260311000010_create_product_mappings.sql` already existed from Story 1.2 with correct schema, RLS, and index
- `ProductMapping` interface already existed in `types/webhook.ts` (from Story 4.1); added to `database.ts` for consistency

### Completion Notes List

- Added `product_mappings` table type to `database.ts` Database interface (Row/Insert/Update) and convenience type
- Created `ProductMappings` client component for admin product edit page — lists existing mappings, add/remove with inline API calls
- Created REST API: `GET/POST /api/products/[id]/mappings` + `DELETE /api/products/[id]/mappings/[mappingId]`
- Validates: empty external_product_id (400), invalid gateway (400), UNIQUE constraint (409 DUPLICATE_MAPPING)
- Extracted `lookupInternalProduct()` from inline webhook code to `src/lib/webhooks/product-lookup.ts`
- Refactored webhook handler to use extracted lookup function (cleaner separation of concerns)
- Lookup logs warning to `webhook_logs` when mapping not found, returns 200 with `ignored` status (AC 4)
- All 205 tests pass, 0 lint errors, typecheck clean, build succeeds

### File List

| File | Status | Purpose |
|------|--------|---------|
| `src/types/database.ts` | Modified | Added product_mappings table type + ProductMapping convenience type |
| `src/app/admin/products/[id]/page.tsx` | Modified | Integrated ProductMappings component |
| `src/components/admin/ProductMappings.tsx` | Created | Admin UI for managing external product ID mappings |
| `src/app/api/products/[id]/mappings/route.ts` | Created | GET/POST API for product mappings |
| `src/app/api/products/[id]/mappings/[mappingId]/route.ts` | Created | DELETE API for individual mapping |
| `src/lib/webhooks/product-lookup.ts` | Created | lookupInternalProduct() extracted from webhook handler |
| `src/app/api/webhooks/payt/route.ts` | Modified | Refactored to use lookupInternalProduct() |
| `tests/unit/lib/webhooks/product-lookup.test.ts` | Created | Unit tests for product lookup (4 tests) |
| `tests/integration/admin/product-mappings.test.ts` | Created | Integration tests for mappings API (5 tests) |

---

## QA Results

_To be filled by QA agent_
